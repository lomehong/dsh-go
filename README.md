# dsh-go — DSH 宿主的 Go 重写工程

对照官方 `deepseek-ai/deepseek-harness` 标签 **dsh-v0.1.2-alpha.1** 的源码语义，把宿主运行时从 TypeScript/Node 重写为 Go；Web 客户端与插件 SDK 契约保持 TypeScript。语言分界画在 wire 契约上，不在实现上。

## 已完成包（全部 `go build/vet/test` 绿）

| 包 | 对照源 | 内容 |
|---|---|---|
| `cordis` | `vendor/cordis` + `docs/cordis-primer.md` | 内核：服务注册表（祖先链解析）、inject/provide（延迟满足）、effect（LIFO disposer）、waterfall（next() 委托/短路/双调 panic）、Dispose（错误收敛+panic 遏制）、Mount fail loud |
| `cordis/loader` | `vendor/include`、`packages/boot/app-boot` | cordis.yml 条目列表解析（两遍式解码，group 标志先于 config 出现也正确）、`!!js` → `RawExpression`、patch 算法忠实移植（insert 顶层/入组、override **整字段替换**、miss 仅告警、后行 patch 可命中先行插入行）、`./`/`../` 名字锚定 patch 文件目录为 `file:///` URL、JS 求值器挂钩（默认 fail loud） |
| `host/webserver` | `packages/host/webserver` | 路由 first-claim（exact 优先、prefix 最长前缀胜出且带 `/` 段边界——对齐官方 match）、fallback 席位、升级路由（exact 路径 hijack 派发、socket 归 registry 追踪并由 Close 显式回收——node 不把升级 socket 计入 closeAllConnections 的同构语义）、结构化 index 注入（`webserver/index-inject` waterfall 收集行 + RenderIndexInjections head/body 拼接 + `__DSH_BOOT_READY__` 尾注 + tapIndex 逃逸口）、重复注册拒绝、失败遏制（错误/panic → 400，已提交响应不改写）、disposer、cordis 装配。诚实降级：gzip 压缩未移植（官方 config-gated 默认 none，消费方未上无压力面）；listen 生命周期归 Go 惯例的 http.Server 消费方所有 |
| `settings` | `packages/settings/settings` | 命名空间注册、解析层叠（schema defaults → base → user）、Update 稀疏合并 / Replace 整段 / Mutate 路径操作+expectedRevision、校验拒绝整次写入、深相等不发事件、事件携带 next/prev/source、watcher panic 遏制 |
| `settings/file` | `packages/settings/settings-file` | 用户文档 YAML 持久化（temp+rename 原子写、重试）、宿主写自动落盘、轮询 watcher 推送外部编辑（provider source）、被拒段落保留最后好值 |
| `credentials` | `packages/credentials/credentials` | Ref（POSIX 环境变量名语法）/ Key（`<scope>/<id>`，两段小写连字语法，与 Ref 文法不相交）、api-key/grant 记录联合、Notifier 包容派发（普通失败记日志，INVARIANT 编码错误在全部监听器跑完后重抛）、空值=处处缺席、MemoryProvider（ModifyRecord 串行化 RMW） |
| `llm` | `packages/llm/llm/src/{types,message,error,call-config,brand}.ts` | 提供商中立消息/内容块/流块词汇（wire JSON camelCase 逐字对齐）、TokenUsage 不相交计数、LlmCallConfig 字段级相等、BoundContextSummary（rune 计）、HarnessError（code+cause）、上下文溢出/配额分类器正则忠实移植、ErrorChain（去重内嵌 cause、`[a; b]` 聚合）、UUIDv4 MessageID |
| `session`（核心层完成） | `packages/core/session/src/{types,json,surface,request-header,index}.ts` | SESSION_FORMAT_VERSION=0（无兼容承诺）、12 事件词表 + fail-closed known-event 守卫 + RegisterEventType 插件扩展、SurfaceOp 双形态 wire（`"append"` / `{op,start,end}`）、Event 信封自定义 Marshal/Unmarshal（`sourceEventSeqs` 在场空数组语义保留）、Session 类（append：canonical-JSON 强制 + surface intent 契约 + 消息形状校验 + 锁内提交锁外派发；seed 重放 + end-seed 标记只写一次；派生消息缓存按 replaceGeneration 重建；request-header/context 增量折叠）、SurfaceManager（append/replace 折叠、sourceEventSeqs 溯源校验、tool/result 仅 content 可改写）、Store（Create/Enter/Announce 发布事务、veto 回滚配对 detach、事件流/flush allSettled、Fork 边界 + 开回合拒绝） |
| `session` 存储层 | `packages/core/session/src/{seq-ranges,chunk-rows,repair}.ts` | sourceEventSeqs 无损区间编码（≥3 连续段成对，双类型平面兼容）、assistant/chunk delta 行打包（text/reasoning/tool-call-chunks，MIN_RUN=3 格式常量、白名单精确形状、混合 run 不打包、dt 差分时间戳、安全整数重建界）、decode fail-loud（malformed row 拒绝整行）、InterruptedTurnClosers（TOOL_NOT_STARTED/TOOL_OUTCOME_UNKNOWN 合成收尾、逐字固定的模型可见文本、复用最后时间戳） |
| `session/persistence/jsonl` | `packages/session/session-persistence-jsonl/src/format.ts` | 路径安全：SessionID UTF-16 code unit 注入式 `~XXXX` 转义（`.`/`..` 特判）、projectKey 有损 slug（连续分隔符合并 `-`、包 `--`、截 251）、header 行 `type:"session"`（delegationDepth 恒在、retired 字段拒绝）、**版本拒绝先于结构校验**（SessionFormatUnsupportedError → "upgrade the harness"）、增量 Scanner（torn tail 报告不截断、损坏后出现 turn/end 才致命、seq gap 回滚保留前缀）、List 仅读 header 行 |
| `llm` 运行时 | `packages/llm/llm/src/{index,adapter-failure,api-key,retry-policy}.ts` | Adapter 注册表（全有或全无 prepareRoutes/commitRoutes 单临界区换路、DUPLICATE_ADAPTER/INVALID_ADAPTER）、RegistrationHandle（Replace 原子换路由、空集合法保活、REGISTRATION_DISPOSED）、`llm/stream` waterfall（先注册者最外层、短路合法、hook 失败保持抛出）、ListModels 咨询式目录（INVALID_CATALOG 拒坏/重复元数据）、resolveModelInfo 归一化（INVALID_MODEL_INFO/CONTEXT/MAX_TOKENS/REASONING）、ResolveCallConfig（UNSUPPORTED_REASONING_EFFORT、adapterDefaults 物化标记）、PrepareCall 一次性派发（INVALID_PREPARED_CALL、跨代配置漂移拒绝）、adapterStream 边界（setup/迭代失败→单个终止 finish 块、panic 遏制=官方 try/catch、ctx 取消/ABORTED→aborted 类）、外来 replayState 剥离、provider retryPolicy 注册期捕获（默认 normal 5 次/500ms/10s/jitter 0.1）、API key 传输法（打印 ASCII、trim、消息不回显密钥） |
| `llm/deepseek`（文本路径+插件装配完成） | `packages/llm/llm-deepseek/src/{types,serialize,translate,sse,adapter,index}.ts`（image/Files-API 变体延迟） | DeepSeek wire 词汇逐字对齐（snake_case：`reasoning_effort`/`stream_options.include_usage`/`thinking.type`/`tool_call_id`）、thinking/effort 决策矩阵（session-title 强制 disabled、off→disabled、disabled+effort 拒绝、非法 effort fail loud）、assistant 重放（content 恒 `""` 不为 null、CoT passback、tool_calls 数组）、tool-result→独立 `role:"tool"` 消息 + `(no output)` 占位、spec-strict SSE 分帧（空行才派发、CRLF/BOM、comment 只作活动回调、EOF 无 `[DONE]`→STREAM_CLOSED）、translate 有状态块装配（空 reasoning 不开块、finish/usage 推迟到 `[DONE]`、usage latest-wins、空块 stop→EMPTY_RESPONSE、未知 finish_reason→大写 code 的 error）、mapUsage 缓存命中相减（不相交计数、total 一致性规则）、HTTP 适配（每请求冻结连接快照+密钥同代解析、attribution User-Agent、httpErrorCode 状态映射、Retry-After/x-request-id 捕获、空闲看门狗→TIMEOUT、中断→ABORTED）；插件装配层：cordis.yml config 分层于可选 `llm-deepseek` 用户设置节（Schema.Validate 复用 ResolveAdapterOptions，拒写节不落地）、API key 经可选 credential 接缝逐请求解析（托管存储优先于环境；无接缝时环境即全平面；MISSING_CREDENTIAL 类型化错误）、keep-last-good 记忆（无效快照按 canonical JSON 幂等告警、续供最后好配置；静态组合装配期 fail loud）、唯一注册期捕获事实 retryPolicy 变更时 `Replace` 原位重注册（独立 replace 锁避免与适配器重入 Options 死锁）、configurable-provider 发现表注册（重复路由 fail loud） |
| `session/persistence`（协调器完成） | `packages/session/session-persistence/src/{coordinator,preparations,write-behind,index,revision,errors}.ts` | PersistenceCoordinator：per-id 串行链（互斥队列、错误不投毒、公共方法不互调）、Create 懒物化（首批次原子建头+写入、持久化冲突/重复 id fail loud）、Append 连续性契约（seq 续接游标、遗留形状写入端拒绝、未知类型仅读端拒绝的权衡注释）、prepare/load/inspect 修订版收敛重试（日志稳定一轮读检才提交）、prepareCore 撕裂修复序列（tornMarker+合成 closers→CommitRepair→修订变更强制重读）、SessionPreparations（loading/ready/committing/reserved 相态机、in-flight 冷读共享、独占预约、LRU 逐出、assertWritable 拒预约期间写入）、InterruptedTurnClosers 接线、onCreated 四分支（认领无主状态需 cwd+seed 双匹配、HMR adoptLivePrefix 只截断不关回合、id 冲突 fail loud）、SessionWriteBehind（固定合并窗口、后台写入 over-budget 续跑、失败保留+automaticPaused、flush barrier 并发合流）、BorrowSession 观测租约（prepared 固定源 pin/unpin、live 拾取、修订过期回退）、Dispose 静默排空+后端关闭、ReadFrom（seek 钩子缺省时顺序回退切片、legacy 前缀需求回退全读）、格式拒绝词汇（upgrade the harness 方向感知、raw log 路径附注、Corruption 与 Unsupported 区分）、legacy 迁移（steering→user/message、turn 起止旧信封、pre-identity 消息 legacy-message:id:seq 身份、tool-result 继承 replacement 目标身份） |
| `session/projection` | `packages/session/session-projection/src/index.ts` | Registry 急进驱动：注册单元（纯同步折叠 + 可选 client view + stateVersion）、registration 引用计数（同 key 同版本共享、版本冲突 fail loud、最后一个注销者移除单元与缓存）、per-session 水位缓存（session/created 预置、`session/event` 驱动、迟到注册在首次触碰时全量折叠历史）、引用同一性变更门（Object.is 语义 → Go 指针身份）、变更馈送（仅 wire 单元、仅在监听者存在时）、StateOf/Snapshot（cursor 物化、asOfSeq = cursor-1、keys 选择）、CachedSnapshot（仅已物化提示、最低公共水位）、Checkpoint（JSON 往返即脱离克隆兼 plain-JSON 契约检查）、RestoreFloor（最低可用水位的"减一锚点"——尾读证明存储日志仍延伸到该处，收缩日志拒绝而非供陈旧行）、ViewCheckpoint（零 I/O 阶梯：ver 匹配才 view，坏行缺席不报错）、Restore（可用行跳过前缀折叠；baseSeq>0 且行不可用 → "re-read from seq 0" fail loud；seq 空洞 fail loud）、Hydrate（完整剪裁短路、安装 refreshed 行、普通 live 驱动恰好前进一次） |
| `session/projectioncache` | `packages/session/session-projection-cache/src/{index,spec}.ts`（storage-domain KvTable 以 Store 接缝注入） | 持久投影缓存：`(sessionId → {identity, rows})` 整记录替换、生命周期身份绑定（createdAt+cwd；id 是槽位不是生命周期——无关记录丢弃不迁移）、三个强制写点（session/created、turn/end、session/disposed）+ 计数/间隔节流（time.AfterFunc、markClean 清定时器）、Durability barrier（先 sessions.Flush 后 put——崩溃只能让缓存落后日志，绝不可能超前）、fail-soft 写路径（warn + 自愈）、ColdSnapshot（身份校验行播种 + 失败软化即发即忘回写）、HydratePrepared（毒行 → 从精确日志重试，陈旧 schema 不能让有效会话不可读）、CachedSnapshot（最低已服务水位 = 单一剪裁） |
| `session/persistence/jsonl`（后端适配完成） | `packages/session/session-persistence-jsonl/src/{index,format}.ts`（zstd/Win32 变体延迟） | JSONL PersistenceBackend 适配器：跨项目作用域按 id 扫描定位（LoadStored 只收 id）、非变异 torn-tail 报告（marker=truncate-to 偏移，截断归协调器 CommitRepair 所有——官方语义，此前 Store.Load 急切截断已改为报告 TornTail）、stat 身份修订 `jsonl:size:mtimeNs`（dev/ino 无 Windows 便携等价，已记录偏差）、首批次 header+seed 单次写入原子物化、Create 拒绝覆盖已存日志、方向感知格式拒绝（upgrade the harness）携带 raw log 位置向上透传、ListSnapshots 修订稳定性、ReadStoredRaw 原样导出 |
| `tools`（注册表+执行管线完成） | `packages/core/tools/src/{json-schema,schema,index}.ts`（PTC `run_code`/presentation 渲染器待续） | 强制 JSON Schema 子集：未知关键字拒绝而非放行（逐路径违规收集、显式栈遍历 + 环检测）、单标量 `type`/`oneOf` 互斥（≥2 分支、禁兄弟关键字）、对象 `properties`/`required`/布尔 `additionalProperties`、数组 `items`、类型正确标量 `enum`/`const`（-0 拒绝、const ∈ enum）、注解仅携带不计校验、对象根断言（structured output）、值校验器（逐路径违规、oneOf 恰一匹配、无损 JSON 门、根路径空哨兵 → `arguments`、根下子属性裸键）；作者 DSL：typed `ValueSchemaSpec`/`PropSpec`（编译期挡住未知键，`required` 只在属性层可表达）、`json` 节点 → 仅注解、object 显式 `AdditionalProperties` 开合强制、编译后过子集断言（与官方 compile→assert 双层一致）、`ValidateArgs`/`ToolArgsError`（INVALID_ARGS）、`DefineTool`（timeoutMs 正有限、参数在入体前校验、并发分类器对无效参数回落 false）；作用域注册表：显式 `ScopeKey` 链（最近优先阴影、own 层注册豁免自身过滤器但受祖先过滤于下游）、限制跨链相交（allow∩deny、未知名 fail loud 列出已知全集、保留名拒绝）、单调 guard（waterfall 允许之后才裁决、注册不发 tools/change）、`PresentAs` 每 scope 单声明（祖先声明覆盖后代）、`ExecutionMode`（精确 `true` 才 parallel）、`Schemas`/`WireSchemas`（克隆投递、knownNames 排序、非 native 模式无代码运行时 fail loud）；执行管线：create（registry 铸 token、参数无损快照冻结、**模式折叠在策略管线之前终结**——pre-execute/approval/guard 永不观察必败调用、折叠+已中止保留 finalizer 而非拒之）、prepare（caller 取消→final、pre-execute waterfall→allow/deny/ask、approval seam 无缝/无 agent 降级 deny、四种 outcome 逐字文案、approval cancel + caller 取消→aborted-before-dispatch、guard 首个拒绝获胜、post-gate 取消→post-result）、dispatch（tools/execute 包装器只换信号视图、registry 重熔 caller 信号（同步预检已中止信号）、UNKNOWN_TOOL 仍走管线让策略观察全部到达注册表的名字、body panic/错误→canonical error、fused 取消压过成功→ABORTED）、normalize（wrapper 自制结果过输出契约重验重渲重铸 canonical）、deferred context 前置于 policy contexts、post-execute（accept 可换单一投影、value 替换重过契约、block=feedback→isError 且丢弃 body deferred context、双替换/败值替换 TypeError 遏制）、finalize（物化→definition `finalizeContent`→再物化→tools/result 包容通知，物化失败降级 canonical 错误）；变更通知（tools/change=注册/限制/presentation，guard 除外）；`tools/result` 按 scope 过滤；`ShapeDispatchLog` 瀑布（监听器失败回退原始内容） |
| `scope`（共享内核） | `packages/core/scope/src` + `@deepseek-ai/dsh-scope` | 作用域键链（祖先链最近优先、tag 准入：未打标全局准入/打标仅键+祖先）、NamedEntries（重名调用方错误工厂、插入返回幂等 undo、Map.set 语义=阴影保留首插位置）、AnonymousEntries（符号键、等值各自独立、活跃旗标墓碑）、Merged 有序合并视图、泛型 Layers（全局急建、读不建层、空层回收、Mutate 返回 disposer+changed 标志）、MergeLayers（全局后链最远优先）、WaterfallEvent（scope 过滤派发、双 next panic 逐字） |
| `systemprompt` | `packages/core/system-prompt/src/{index,invariant}.ts` | FIRST_PARTY_SECTION_ORDER 稀疏常量（相邻差 ≥10）、`harness:identity`/`deployment:persona` 默认节、`toolOrder` 构造期校验（重复名/必需 rest 标记逐字）、orderTools（listed 顺序 + rest 处字典序插入、保留名拒绝、未知名 fail loud 列已知全集含 "(none)"、listed 名可被限制缺席）、compareNames=码点序（UTF-16 差异域已注释）、严格插值矩阵（坏名/未知名（按首插序列已知名，空= "(none)"）/未定义值 fail loud、后随 `}}` 的孤立 `{{` = malformed（16 窗口、`…` 在引号内）、无后随 `}}` = 字面散文、替换值不重扫）、renderPrompt（去空节、双换行连接）、renderContextSections/joinContextSections（快照头逐字）、节排序 order→name、>1 complete 节 fail loud（逐字）、contexts 仅按 order 稳定排序（并列保插入序）、tool provider 每 assembly 求值、schema 参数 JSON 往返深克隆、knownNames 显式优先默认 schema 名、runtime context 抑制器（任一即可、独立 dispose）、system-prompt/assemble scope 过滤 waterfall（返回值权威、complete 节随后恢复为唯一节、抑制时 contexts 清空）、OnChange 注册/dispose 均通知、ValidateAssembly 不变量移植 |
| `agent` | `packages/core/agent/src/{index,types,runtime-types,inbox,consumed-work,model-selection,dispatch}.ts` | AgentRegistry 生命周期：Enter（id=sessionId 强制、`agent "x" is already registered` 权威碰撞界、id 不匹配逐字）、Announce（创建监听同步失败=否决→调用方以 detach 回滚；announcing 期间 detach 请求延迟到派发收束；重入/未知条目逐字错误）、detachEntered 条目身份终结界、`agent/created`/`agent/disposed` 载荷配对（回滚的已宣布条目仍发 disposed）、Get/IsOwnedBy（运行时属主≠持久谱系）/List/Roots 注册序；SubjectEventBus 融合派发器（subject 注入 + scope 载体不可分叉；emit 全员运行逐监听遏制（错误+panic 记日志不饿死后者）、emitVeto 首错停止、serial 顺序 bail、waterfall 首注册最外层；未打标听全局、打标按 dsh-scope 祖先准入）；initiator 界（AsyncLocalStorage → context.Context 值显式线程化：With/Without/Current/Require、closing 拒新界、DisposeInitiators 等待在飞界排空后失效、逐字 `no initiating agent is active`/`agent initiator scope is disposed`）；工厂槽（`no agent factory registered (load an agent-loop plugin)`、重复注册 `an agent factory is already registered`、effect 化 disposer）；Inbox 持久投影（`agent/inbox/spliced` 注册进 session 词表 fail-closed、seedLength 起重放=fork 不继承父待办、JS splice 语义截断归一化（越界钳为 no-op、不落事件）、双列表身份唯一 `message "x" is already pending`（next-turn 先于 next-step）、durable 先提交后改内存、claim 纯删除+逐条 claimed 通知、clear 先 next-step 后 next-turn、outcome=canceled 只标纯取消、replace=discarded+inserted、损坏持久 splice 播放期 fail loud 带 seq 归因）；foldConsumedWork 单遍折叠（stepped→end、claimed 仅 completed 不记账、canceled 且零插入=droppedUnrun 粘滞到下一个记账 turn/end、合并可扩展 end 缺省记账）；InstallModelSelection（assembly 前 Current 快照→assembled、provider/model 注入变量集（Clone 不污染原装配）、agent/request 以 assembled 覆写 config、缺席 effort 清继承 effort、并发换型下一步生效不劈开两表面） |
| `llm` 块装配器 | `packages/llm/llm/src/assembler.ts` | BlockAssembler：partials 按 index 插入序、delta-only 协议容忍（无边界也能成块）、block-end 权威且冻结（首闭获胜、迟到 delta 忽略防畸形流膨胀）、tool-call 无 id 时 `call-<index>` 兜底、assembled 单一 keep/drop 决策（max-tokens 丢弃 tool calls + replay envelope 同位修剪、长度不齐丢弃整个 envelope）、InterruptedBlocks（仅闭合/未闭合的 text/reasoning 且非空白；tool calls 一律省略）、Usage/Finish 缺省 `{kind:"stop"}`、Message 带模型 source（provider/model/marshal 后 replayState） |
| `agent-loop`（核心驱动完成） | `packages/core/agent-loop/src/{agent,tool-calls,runtime-context,constants,index}.ts` | ReactLoopAgent 三相驱动（idle/maintenance/running、状态派生 status、wake 闩锁：maintenance 或中止后 activity 唯一重放点、disposed cause 永不闩锁）、turn/step 括号（turn/start+end 每退出路径必有终结、step/end 失败也闭合、claim 边界 user/message 落日志（append 面、无 sourceEventSeqs——与官方一致）、blocked/completed/max-tokens 粘滞/aborted(cause)/error(NormalizeLlmFailure) 全终结词汇、agent/turn-stopping 仅在无 steering 时发）、preStep（claim→Assemble→RenderContextSections→runtime-context 投影（非零才并入决策消息）→pre-step 瀑布（enter/reject）、reject=blocked 终结）、buildRequest（路由=AgentOptions、persisted effort 仅同路由且非 adapter 物化、requestHeaderLogged 前种子配置 vs 后 requestProposal 剥离 adapter 默认、空 provider/model 逐字错误、PrepareCall 失败分类（NO_ADAPTER 中间件放行/其余抛出）、canonical header 记账（initial/resume/change(+startsSeries)/series/不落）、requestContext 经 ResolveModelInfo（解析失败缺窗口）、request/ctx 变更才落）、step 主环（流块逐块 assistant/chunk 落日志、abort→InterruptedBlocks 中断消息（sourceEventSeqs=chunk seqs）、finish error/aborted→request-error 瀑布（Retry 重入、否则 LlmError 结构化）、max-tokens 终结、tool calls→调度器）、并行工具调度（maxParallelToolCalls 滚动池、parallel 分类障碍（重分类不跨组）、模型顺序 commitReady 提交、abort→appendSkippedToolCall（ABORTED_BEFORE_DISPATCH）、调度器失败排空在飞、工具上下文 splice 到 next-step）、runtime-context 投影（owned 快照后向扫描保留、替换面清除、diff 才产消息、plugin source `dsh-system-prompt` + 快照 form）、服务层（`maxParallelToolCalls must be a positive integer`、sessionId/resumeSessionId 互斥、重复精确身份逐字、`agent maxTokens must be a positive safe integer` 含安全整数上界、prepare 公共 memoized 拆卸（disposed cancel→WhenIdle→detach→ctx dispose，先注册后发布）、CreateAgent（seed/header 全字段）/Resume（persistence.Prepare→Release(published)）、配置代理启动（无持久化直接建、持久化恢复缺失才回退新建、失败 reportConfiguredStartupFailure 告警、启动 WaitGroup 并入拆卸）、per-agent provider/model/cwd 提示变量注册在 agent scope、工厂经 registry.SetFactory effect 注册） |
| `interaction/userapproval` | `packages/interaction/user-approval/src/{index,types}.ts` | 审批能力接缝 Service：closed 词汇 allowed-once/rejected/cancelled/unavailable（ rogue 返回归一化 unavailable、throwing answerer panic 遏制在服务内 fail closed 不炸调用方工具调用）、session 策略折叠 `effectiveApprovalPolicy` 后向扫描 last-wins + `approval/policy` durable 覆写事件（append 前校验 `approval policy must be one of "ask" or "never"`、同值幂等不重复落）、`never` 在任何 dispatch 之前决定（answerer 以 prepend 抢注也改变不了确定性拒绝——只有服务自身请求路径能保证）、审计对 `approval/asked`+`approval/decided` 同 id 配对（asked 携带 id/toolName/callId/reason）、open-turn 前置条件（审计对必须被 turn 括号包裹——turn 是持久日志的 commit/replay 边界，turn 间裸事件与 crash tail 不可区分会被重放丢弃；拒绝发生在任何 append 之前）、signal 前后检查取代 AbortSignal 竞速（同步 waterfall：dispatch 后观察到的 abort 判 cancelled、迟到答案按构造丢弃）、answerer waterfall 按 agent scope 准入（agent 打标监听只答自己的 agent）、`approval:policy` 动态上下文段（order 115、逐字 never/ask 两句模型可见陈述、完整现值随保留历史之后传播不打爆缓存前缀；Go 侧由组装点在 agent scope 注册——AssembleContext 无 agent 主体）、ToolsSeam 适配 tools 运行时的同步审批面（scope→agent 解析失败 fail closed、词汇一一对应）；Go 适配：cordis `ctx.approval` → 显式构造器绑定 registry（scoped bus + 身份解析） |
| `interaction/userquestions` | `packages/interaction/user-questions/src/{index,types}.ts` | 用户提问接缝 Service：`ask_user_question requires at least one question`（EMPTY_QUESTIONS）、预中止 signal→ASK_ABORTED、answerer waterfall `user-questions/request` 按 agent scope 准入（无 agent 时全局派发）、`no user-questions answerer accepted the request`（NO_PROVIDER 兜底）、身份界（agent 在场时 `human interaction requires the exact live calling agent`（CALLER_NOT_LIVE——Get(id)≠实例）、`human interaction is unavailable while the calling agent is owned by another live agent`（DELEGATED_CALLER——roots() 才能问人；运行时属主而非持久谱系决定边界：被拥有子代理问了会永远阻塞，谱系会话在新运行时根恢复后可正常问））、presentation intent 校验在 asker 侧（`question x declares intent plan-review whose approve label "y" names none of its options` / `without the detail it reviews`（BAD_INTENT）——intent 只改呈现不改协议、答案编码完全一致）、错误还原（UserQuestionError 穿边界保身份、外来失败遇上中止报告 ASK_ABORTED、监听 panic 遏制走同一错误路径）、外来返回值 fail closed 不伪造结构 |
| `interaction/toolaskuser` | `packages/interaction/tool-ask-user/src/index.ts` | 模型面 Consumer 工具 `ask_user_question`：官方逐字 description + 参数 schema（questions 必填数组、id/question 必填、header/options/multi_select 可选、option label 必填）、输出 schema（answers 数组：id/selected 必填、custom 可选，对象面显式开合）、execute 映射（multi_select→multiSelect、exec.agent 经 AgentByScope 还原活实例、signal 直通）、render JSON 序列化、并发不安全（提问必须独占工具道） |
| `subagent`（基础层+运行时+组装层完成） | `packages/subagent/subagent/src/{types,depth,error,descriptor,descriptor-seed,assistant-output,run-settlement,activation-setup-registry,index,lifecycle,child-agent,continuation.ts}.ts` | 委托深度记账（头部 `delegationDepth` 权威且单调——运行时 `AgentOptions.SubagentDepth` 只能加深不能降低（恢复的子代理新 options 若从零计数会伪装顶层）；非负安全整数校验逐字）、`subagent/descriptor` durable 事件（版本 3、首个事件权威、log-only 不进模型历史、快照显式字段、fold 完整声明校验 one-shot/continuable 键集、外版本→不可分类、描述符种子 staging Session 分配 seq）、终局输出规范选取（最后一条非空 assistant 消息、无消息回退累积 text 流）、one-shot run 结算（completed→final text、本地取消→killed、其余→failed、结果与 dispose 双失败两段 detail 都存活）、Activation setup 注册表（apply 快照→安装→commit、自撤销逃逸→立即释放+作废批事务 ACTIVATION_SETUP_REVOKED、安装器失败保持权威但先全量回滚、disposer 失败全批释放后 ACTIVATION_SETUP_RELEASE_FAILED 聚合）、Capabilities 五标志 fail loud、StopReason 五态+merge-extensible、ContinuableProvider 方法在场即能力；运行时服务（`SubagentRuntime`）：命名 provider 注册表唯一名 DUPLICATE_PROVIDER fail loud、拆卸幂等且 removal 边无 parent 载体全局派发、List 保持插入序（Go map 序随机→slice 记序）、`Start` 先能力门（首个缺失能力即 UNSUPPORTED_CAPABILITY 逐字 `subagent provider "x" does not support the "y" capability`）再 maxDepth/对象 schema 校验再 descriptor 快照（one-shot 模式 label 可选）后委托 provider、发布前 provider 失败无 run 可处置且零生命周期边；生命周期（lifecycle.ts）：start/end 按 runId 配对（identity 只铸一次）、start 同步先发后挂终局观察者（Promise 反应序→Go 先 emit 后启 goroutine）、终局 watcher 基础设施故障→error 边不带输出、`EpochStopReason`（`FoldConsumedWork`：end 缺席→droppedUnrun?aborted:completed、max-tokens→max-tokens、aborted/interrupted→aborted、error→error、blocked→refusal、completed→droppedUnrun?aborted:completed、未知变体→error——recorded failure 胜过 cancellation、unnameable 不算成功）、ActivationObserver（start 记日志边界、capture 只取本 epoch 后缀防冷恢复串台、terminal failure 覆盖自身结局且扣发输出、settle 恰一次配对）；continuation 类型面（ContinuableStartSpec/Start、SubagentReportOptions/Delivery quiet|next-step、SubagentInterruptAuthority user|ancestor 标签联合、SubagentFollowupOptions source 不授权、三种 durable message-source kind）；Go 适配：`ContinuationManager` 显式接缝（官方 `ctx.inject(['agents'])`）——无 manager 时 continuable 操作 CONTINUATION_UNAVAILABLE fail loud、interrupt/drain 为接受式 no-op |
| `workflow`（接缝层完成） | `packages/workflow/workflow/src/{types,runtime-types,index}.ts` + `workflow-worker-thread/src/meta.ts` | 编排接缝 Service Definition：词汇（WorkflowRunID/Phase/Meta 身份块、StopReason 封闭三态 completed/cancelled/error、WorkflowResult（value 仅 completed 有意义；agentsStarted 优雅结算=脚本侧计数、终止路径退化为宿主观测计数）、RunInfo/AgentInfo/AgentEndInfo/ResultInfo 事件数据——end 载荷刻意剥掉 value，观察者拿不到调用方结果的 mutable 别名）、WorkflowError 11 个 fatal 机器可路由码（parse/meta/argument/schema/容量/子代理基础设施/边界值不可序列化/取消）+ fatal 旗标（parallel()/pipeline() 纪律：fatal 重抛杀脚本、普通子代理失败逐项置 nil；host 断言不从脚本面伪造）、六事件 observe-only 词表（start/phase/log/agent-start/agent-end/end，配对契约：agent 按 seq、end 随 result 恰好一次、termination 路径引擎合成 cancelled end）、EventSink 引擎自持监听表逐个遏制（panic 告警 `workflow: <event> listener threw:` 不进引擎路径、后续监听仍跑、undo 幂等）、StartRequest/Run 契约（无效请求发布前失败、result 永不 reject→单次投递 channel、cancel/dispose 有界）；meta 校验（plain JSON 数据绝不求值脚本文本——宿主求值会跑 getter 逃出 worker 超时隔离；未知字段逐点具名拒绝、name/description 非空、phases 逐项四字段校验、一次性 META_INVALID 报全部违规、归一化副本不 alias 调用方对象）；**脚本执行引擎已交付**（`workflow/engine.go`，R10 决策见"语义决策记录"Go 原生脚本域条目：ScriptAPI=agent/parallel/pipeline/phase/log/args、发布前校验四门、agent() cap+派发 fatal、parallel 保序并发、pipeline 无栅栏、settle 恰一次、Dispose 幂等有界、脚本 panic 不死引擎、RunTraceValidator 13 违例面 invariant 全程观察） |
| `todo` | `packages/todo/tool-todo/src/{index,types}.ts` | 模型面整表替换工具 `todo_write`：每次调用追加 `todo/write` 快照到调用方会话（replay last-write-wins、UI 从事件渲染）、非 agent 调用方无属主列表直接拒绝（`todo_write requires an owning agent session`）、描述四段拼装（固定头尾 + 唯一随策略变化的 active-status 从句——allowParallelInProgress 是必需部署选择不是可调默认）、值校验（trim 非空唯一 content、status 枚举、至多一个 in_progress 除非部署允许并行、逐字违规文案、拒绝的调用不落任何事件）、输出 schema+todos 计数（pending/inProgress/completed）、render 逐字 `Updated todo list: N pending, N in progress, N completed.`、`todos` 投影单元（StateVersion 2、todo/write 最新整表、turn/start 清空而 turn/end 保留完结清单可见、其余事件同引用零开销、DecodeState JSON 往返）；并发安全（整表替换幂等可并行） |
| `homepaths` | `packages/util/home-paths/src/index.ts` | 共享路径助手：DSH 单根 home 解析优先级（显式 configured > `$DSH_HOME`（空白=未设，空覆盖绝不解析到 cwd）> `~/.dsh`）、波浪号展开（`~`/`~/`/`~\`）、显示形式（默认 `~/.dsh`、配置 `$DSH_HOME`——绝不回显机器绝对路径）；Go 适配：env 经显式 `Getenv` 接缝注入（官方 ambient process.env 等价）、`canonicalizeWatchPath` 延迟（Go 侧 watcher 后端不同，原生 watcher 的 realpath 规范化随之轮） |
| `identity` | `packages/identity/anonymous-user-id/src/index.ts` | 每 harness-home 匿名用户 id：随机 UUID 持久化为 `.anonymous-user-id` 裸行（无包装格式）、绝不从主机名/网络/git remote 派生、作用域是 home 不是机器（删文件=下次启动换新身份）、按解析文件路径进程级 memo（一次触盘、中途删文件本进程 id 不变）、独占创建写解决并发首发（败者重读采纳胜者；创建到写窗口内仍可能一进程两 id，下次启动收敛）、持久化尽力而为（只读 home 仍返回可用 id，遥测反馈永不阻塞）、损坏文件走重读→覆写梯子 |
| `webhook`（规则运行时完成） | `packages/webhook/webhook/src/{types,brand,index}.ts` | fire-and-forget 规则运行时：三种 opaque 身份（rule/source/delivery，delivery 无内建去重语义）、VerifiedWebhookDelivery（kind 非空、source/deliveryId 非空、receivedAt 非负安全整数逐字拒绝、分发前 `snapshotJsonValue` 无损 JSON 往返即校验即脱离——跨任意规则共享的是冻结快照）、WebhookSessionRequest 七字段（唯一内建动作：建一个根 Session）、Rule 接口（任意可信代码、返回 null=不动作、signal 随注册或运行时卸载中止）、register 逐字校验（`webhook runtime is closing`、id/kind 非空、`webhook rule "x" is already registered`、disposal 后 id 释放）、dispatch 按 kind 路由、返回先于任何回调结算（fire-and-forget）、逐调用遏制（错误/panic 都不逃逸：`webhook: provider="k" source="s" delivery="d" rule="r" failed: <chain>` 告警、 disposal 后停止走单独通道）、注册拆卸=隐藏+abort+排空活跃调用（memoized 幂等）、运行时关闭后注册与分发 fail loud；Go 适配：SessionCreator 接缝替代 createWebhookSession 的服务组合（workspace/preset/permission 服务随对应轮接入，nil creator 对返回请求的调用 fail loud） |
| `compaction`（接缝层完成） | `packages/compaction/compaction/src/{types,brand,checkpoint,tool-pairing}.ts` | 压缩词汇与 lock 事件：`compaction/*` 四事件 log-only 进 durable 词表（start 持锁到 end、编号 owner 严格被 open turn 包住、`turn: null`=回合间的独立手工事务；summary 记录摘要+输入+模型调用事实——provider/model/maxTokens/usage/rawOutput、`llmStreamCall` 标记唯一可溯调用、无标记=不可溯的远程/模板摘要；end 释放锁、error 记失败尝试；prune=无模型 prune 替换的影子价格）、影子价格共享协议（surface replace 由其紧邻的前一条计量事件定价——消费方可配对而无需保留逐节点价格、替换必须紧跟其后同步追加）、位置跨度语义（start 可大于 end——早先范围的位置上落了新高 seq 摘要节点；shadowedSeqs 才是权威集合）、CompactionResult 七字段、checkpoint 溯源（`{kind:"plugin", plugin:"compact"}` 标记+compactionId+可选 sourceCommandId、识别谓词与构造器分离）、工具配对平衡（安全切面派生自当前 surface 顺序的 tool-call/result 内容而非 step 标记；N 条序列 N+1 个切面；增量缓存按 replaceGeneration 失效重建；损坏日志 fail loud 且验证先行不落半推进状态——unpaired result 逐字拒绝、未知 seq 拒绝）；Go 适配：WeakMap → 调用方持有的 owned 实例，内部记录 owner 会话指针检测跨会话复用（测试实修了这一锐边：共享实例曾把 A 会话状态喂给 B 会话） |
| `workspace`（完成） | `packages/workspace/workspace/src/{types,entity,paths,spec,index}.ts` | 稳定 id 的工作区记录（uuid 绝不用路径——路径规范化会重写而引用锚必须稳定）over 已存在目录：`RealpathNormalize` 是唯一同一性 canon（尾斜杠/`..`/symlink 全解析、不存在即 create 的拒绝路径、唯一性=规范化后字符串相等、attach 校验走同一 canon）、实体只持记录快照（每次持久变更后就地换入）且所有写入都过私有 `mutate`（updatedAt 戳一次、失效候选剪枝一次；no-op 经哨兵中止链槽——不写介质不发变更事件；成员判定在写链上因此与排队写入无竞态）、`SessionIDs` 同步过滤（丢头/cwd 非法/canon 不匹配的候选绝不返回，后续任意被接受的变更把过滤候选持久剪掉）、attach 校验梯子逐字（无 cwd、`does not resolve`、非目录、`its cwd resolves to '<canon>'`；已记账 id 跳过重验——cwd 事实首挂时已查且两个输入都不可变）、insertBefore DOM 语义（有锚落其前、无锚追加尾、只有被移动 id 变位、会话/锚未记账抛 typed `WorkspaceMoveInvalidError`、自锚=no-op）、detach 幂等绝不碰会话自身日志、status 实时 stat（目录缺失绝不改记录——可能只是暂时移走）、归档集 layered 在记账上（归档会话保留其槽位、不参与单一属主不变量）；**registry**：强制依赖 sessionPersistence（不可用 peer 绝不冒充空历史提交 initialized 标记）→ 开域→恢复 pending 标记→validateStoredState→未初始化才跑一次性历史引导（按 canon cwd 分组建记录、历史会话并入既有记录、按最新会话降序/路径/先序/ids 排名、两段 setState 先置 initialized=false 再置 true）、header 索引（live 覆盖持久、cwd 判决三分：解析目录/无 cwd/不解析/非目录带 reason）、filtered 候选逐条告警（canon 不同/缺头/非法 cwd 各带具体理由）；create/delete 事务：记录与顺序两写之间先落 pendingMutation 标记（每个操作恰一名）、失败回滚链逐字（record 写败→退 marker；order 写败→退 record→退 marker；双重失败 AggregateError 语义；delete 末段 marker 清理失败只告警——已提交的删除不回滚上报）、操作链每次 create/delete 前重跑恢复、恢复绝不从记录形状猜操作；顺序操作 insertBefore/归档（未名会话=definite miss typed 错误、存储故障照传）；Go 适配：异步 KvTable 折叠为同步键控读写（实体 mutate 只依赖读改写串行化）、enqueueOperation promise 链 → 注册表互斥锁、`ctx.get('sessions')`/`sessionPersistence` → 显式 RegistryHost 接缝、uuid v4 via crypto/rand、`stat` 目录判决同步 |
| `timecontext` | `packages/context/time-context/src/{index,request-zone,timestamp}.ts` | opt-in 请求时钟上下文：合格步骤往请求历史追加 durable、带源归因的时间读数（plugin 源 snapshot user/message、sections 与文本同体）——模型面可见 ⟺ 日志可重建、但读数本身由循环在进入步骤时才落日志；pre-step waterfall 监听先调 next 再把读数垫到其它变换之后（Go：`OnWaterfall` 首注册最外层 = 官方 `{ prepend: true }`）、reject/abort 直通；刷新门（RefreshIntervalMs 非负安全整数逐字拒绝、距上次 durable 注入不足阈值则跳过——查日志含被 replace 遮蔽的注入）；文本三行逐字拼装（采样时钟 `2026-01-02T03:04:05+08:00[Asia/Shanghai]` 长偏移形、UTC 永远 `+00:00` 不用 Z；step 1 基线=model-visible message、后续=step context、elapsed `1d 1h 1m 1s` 组合式整秒）、浏览器时区派生（user-rpc 源的 `clientTimeZone`、Host 规范形校验逐字拒绝 `Mars/Olympus`/非规范形、set→排序→resolved/mixed/missing 三态策略行逐字——missing/mixed 都让模型向用户澄清）；Go 适配：`Intl.DateTimeFormat` 规范化折叠为 `LoadLocation` 成功即规范（LoadLocation 派生区 `String()` 即 IANA 名、进程回退区为 `Local`）、`llm.NewUserMessage` 实修为按调用方 source verbatim（此前强制 `kind:"user"` 吞掉了 plugin 归因）、时钟走 `nowMillis` 测试接缝 |
| `planmode` | `packages/plan/plan-mode/src/{index,types}.ts` | plan 模式=logged per-agent 协作状态：生效状态从日志折叠（`plan/mode` log-only 非表面、last-wins、无记录=inactive——resume/fork 无需活镜像即恢复）、用户选择保持 pending 直到下一个被接受的 turn 内 pre-step（open-turn 判定=有 turn/start 无 turn/end；agent status 穿过回合后 checkpoint 保持 running 时没有后续 turn 内 pre-step，这是 idle 信号）、`Set` 四态（committed 立即落日志+`inject` 叙事/queued 等下个 pre-step/cancelled 相反 pending 被清且日志已匹配/noop）、写入只在 append 成功后清 pending——失败的 durable 写保持可重试；pre-step 在会话发布之外追加 log-only 事件、append 失败告警并保持 pending 且策略绝不挡步骤、`narrate` 旗标（用户选择 true、exit 工具 false——其结果已叙事）、叙事只在最后一条 request/header 描述相反模式时构造（`The user switched this session to plan mode.` plugin notice、自带 summary）、同一步重试复用其装配、exit 工具保持注册使工具目录跨转换稳定（仅提示段变化）；`plan` 投影单元（StateVersion 2）：command/run 记录 logged /plan 选择、配对 settlement 只留成功选择、`plan/mode` 记录并清空——pending 成为纯 replay 量（重启/他标签/冷读全部从日志恢复）、wire `{active, pending}`；Go 适配：exit 工具与 `/plan` 命令随 user-questions/commands 能力轮接入（本帢单元折叠已按官方语义实现）、pending-intent WeakMap → 控制器持有的 keyed map（条目消费即删）、TextProvider 无 agent 形参 → 按会话作用域注册节（`plan:policy`、OrderPlanPolicy=500） |
| `storagedomain` | `packages/storage/storage-domain/src/{spec,domain,index,events,error}.ts` + `storage/src/backend.ts` 契约 | domain 数据形态（`ctx.storage.domain`）：schema 校验、发 change 事件的 KV 域——读取同步取自内存权威态；每次写入先等 backend 持久化、再改内存、再发 `domain/changed`（backend 写失败内存不动——读与介质零分歧；事件值=发射时的内存态、按写序、绝不带旧值）；`domain/changed` 监听失败按监听者包含（告警不传播进写路径）；规格词表：`DefineDomain` 在装载期 fail loud（域名/表名过 `UNIT_NAME_RE`、版本非负整数、layout 枚举、global 校验器必须拒 `null`——null 是介质"从未写入"哨兵否则无法往返）、`DescriptorOf` 投影到 backend 单元描述符；域运行时：global 单例（首次 Set 才物化、存量 null 哨兵→服务 initial 不落盘）、表句柄（Get/Entries 快照迭代/Size/Put/Delete 幂等 false 不写不发事件/Update 原子读改写——`missing-key` 逐字）；facility：单开（reserved 集合并发开同名声败 `already-open`）、路由（默认 backend + 每域 routes 覆盖、未注册→`backend-not-found`）、开域五步（已开→路由→开单元→loadAll 全量 zod 校验 `invalid-record` 带槽位 `record 'x' in table 't'`/`global`→构造；caller 持有句柄自行 close）、`Close` 排干后释放+onClosed 严格在拆除完成后跑（名字释放可重开）；Go 适配：backend 为同步接口（Go 文件后端同步写）→ 源 per-domain 异步写链折叠为互斥串行 + Close 排干、storage-json/sqlite 文件后端与 storage hub（注册表+服务键）随后续轮接入（本带 `MemoryUnit` 契约实现支撑测试与组装）、zod schema → ValidateRecord/ValidateGlobal 闭包跑在同等 durable 读边界、并发开域用 reserved 集合复刻官方单开保证 |
| `storagejson` | `packages/storage/storage-json/src/{index,backend→index,atomic,format,single-unit,per-record-unit}.ts` | JSON 存储后端（backend `json`）：配置 root 下每单元一份人类可读文档——`single` 布局=`<root>/<name>.json` 整单元文档（unit 头戳 name+version）、`per-record` 布局=`<root>/<name>/` 目录树（每记录独立版本戳文档 `<table>/<key>.json` + `global.json`，一次写只重写一条记录）；发布协议=同目录临时文件（独占 0600、fsync）→ `rename()` 原子替换（POSIX 后再 fsync 父目录；Windows 跳过目录 fsync；替换语义=单元文件每进程恰一写者、last-write-wins 正确）；`single` 单元：内存态权威、每写原语变更后整文件原子重发布、发布失败回滚内存变更（被拒写不得驻留内存或搭下次发布顺风车）、删除幂等、缺文件=空单元（物化推迟到首写）；`per-record` 单元无状态设计（目录即介质）：坏/陈旧记录文档（损坏或版本戳不符）读作 absent 不 brick 整单元——版本升级弃置陈旧记录而非迁移（整单元格式因恰一份文档而改为拒绝）、path-safe key 校验逐字拒绝、空树从 legacy 整单元文件引导迁移（只拷已声明表记录、legacy 文件原样保留、foreign/损坏 legacy 不动、legacy global 不迁移——照源）；backend：描述符名校验 `malformed-medium`、双开=caller bug（`already open; a unit has exactly one live handle`）、Close 排干打开单元且 in-flight 开门将发即时关闭（不越过 Close 发放活单元）；Go 适配：文档键序 sorted（encoding/json）而源为插入序——键序是可读性细节非解析契约、值经 serialize→parse 重格式化与源 JSON.stringify 同理（测试按 JSON 相等断言）、单元互斥锁只为 Go 内存模型（写排序仍归 caller 域层）、磁盘目录 fsync 仅 POSIX |
| `commands` | `packages/interaction/commands/src/{index,types,brand}.ts` | 人机命令注册表（交互 UI 适配器共享）：命令名 `[a-z][a-z0-9_-]*`、`ParseCommand` 精确切分（名字后界=行尾或水平/垂直空白，`/pl an`=命令 `pl` + rawInput `" an"`、rawInput 含分隔空白逐字不规整）；注册 fail loud（坏名/空空描述/nil handler/空空白 hint 逐字拒绝）、层内名字唯一（全局重复指向 per-agent 挂载诊断、scoped 重复 `already registered in this scope`）、scoped 影子对所属 agent 遮蔽全局、`List` 名字排序输出 handler-free 描述符、`Register` 返回精确注销 disposer；执行生命周期=已 logged：`command/run`（结构化 payload：name+verbatim rawInput——消费者不再 re-parse；`RecordInput=false` 时省略 args 因为权威 domain 事件拥有输入）在 handler 前追加、`command/done` 在结算后追加（handler 抛错/中止结算为 `kind:'error'` 带渲染失败文本；成功可带 `sourceEventSeq` 指向更丰富的权威事件）；两者皆 log-only 直接追加（无 turn 包裹、持久化在普通 checkpoint 排干）；admission miss（语法/未知名）不落任何日志——从未进入 handler；`command/run` 追加失败响亮失败、handler 失败路径上的 `command/done` 追加失败被包含（handler 自身错误保持为上报失败）；配对 id=`cmd-<实例token>-<序号>`（单调、跨重启不重复）；图片准入门在注册表不在 composer：未声明 images 的命令收图/无 attachment store 各结算为错误结果且 handler 不跑、被拒批次不发布任何 durable 对象；准入门后再次检查取消（慢存储期间取消的请求不得让 handler 改状态）；`commands/change` 注册表通知非否决制（观察者失败逐个包含）；Go 适配：abort signal → ctx（已取消在 admission 前抛、handler 运行中取消=goroutine 弃置同源 withAbort 语义）、handler 为同步 Go 函数、Invocation 携带接收 session（源为 Agent——Go handler 拿生命周期记录所属的 session）、scope key 显式参数（tools 运行时模式）、attachment store 为可选 seam（未组装即逐字 unavailable 文案）、`commands/change` 走运行时自有监听者表（cordis emit 语义逐个包含） |
| `interaction/permissionpresets` | `packages/interaction/permission-presets/src/{index,types}.ts` | 用户可见权限预设，横跨独立的 sandbox 模式与审批策略旋钮：`permission/preset` 事件记录所选预设为 durable log-only 用户意图（不入模型转写；两预设共享同一 knob 束时保留用户选择——折叠后仍匹配的最后选择赢共享束平局，否则声明序首个匹配，都不中=`custom`）；切换=记录预设→仅对变化旋钮走各自 canonical setter（再选当前生效预设零追加；共享束切换只补预设事件）；新会话 pin：全空日志→用户默认预设+两旋钮齐发；seeded/部分初始化→保留生效旋钮只补缺失事实（推导 `custom` 永不落为预设事件——`custom` 从不是事件 payload，仅是派生显示值）；读取侧=`permissions` 会话投影（三旋钮事件折叠、视图按组合默认推导 select：声明序选项+恰好 current 时才追加 `Custom` 选项、state schema 严格拒绝未知字段）；写入侧=`/permission` 命令（裸行=报当前值+可用名单、未知名=错误结果 `unknown preset "x" (available: ...)`、有效名=应用+`preset x`）；服务构造 fail loud：`custom` 保留名、未约束执行器（无 sandboxMode——预设捆绑沙箱模式，装配错配置）、组合默认无匹配预设要求显式 defaultPreset；默认表 `workspace-write`(workspace-write+ask) 与 `danger-full-access`(danger-full-access+never)；settings section 装载与 `session/created` 钩子随装配轮接线；Go 适配：`ctx.shell.sandboxMode` → 显式 `SandboxDefault` 构造参数（空=未约束逐字报错）、`sandbox/mode` 旋钮读写助手暂驻本包（dsh-sandbox-policy 的读写面，shell/sandbox 能力轮再归位）、JS 对象键序 → Presets map + Names 声明序切片、投影 fold 不可变值语义（Go 值返回即同引用 change 门） |
| `storage` | `packages/storage/storage/src/{index,registry,backend,error}.ts` | 存储 hub（`ctx.storage`）：命名 backend 注册表 + 已挂载数据形态 facility 表——hub 自身零 IO（backend 拥有介质、数据形态拥有语义）；多 backend 并存、谁服务谁是消费者配置（如 domain 层路由表）而非 hub 全局选择；`Register` 幂等 disposer、重复名 `duplicate-backend` 逐字、**disposal 不关 backend**（拥有插件先注销再关）、陈旧 disposer 身份护栏（dispose 后重注册同名 successor 不被旧 disposer 移除）；`Get` 未知名 `backend-not-found` 带已注册名单（空=none）；`StorageBackendServiceKey(name)`=`storage.backend.<name>`——domain 形态 provider 注入此生命周期键使激活不与 backend 注册竞争；`Mount`/`Form` 同款护栏与 `duplicate-mount`/`form-not-mounted` 逐字、`domain` 类型化便捷访问器（domain 层插件装载后出现）；hub 错误词表与 backend 面词表（`version-mismatch`/`malformed-medium`/`closed` 归 storagedomain UnitError）分离；Go 适配：`ctx.storage` service property → 装配组合的显式 Hub 值、`StorageForms` 声明合并 map → string 键 facility 表 + 类型化 `Domain()` 访问器、注册表名单快照排序（源为 Map 插入序——确定性诊断细节非契约）、生命周期服务键随 loader 轮接入 |
| `tokenmeter`（计量核心完成） | `packages/llm/token-meter/src/{types,estimate,surface-fold,route-pricing,index}.ts`（usage/pressure/breakdown 投影单元 + turn-usage 折叠随投影轮补） | 回放感知的单例计量服务：固定密度启发式（4 字符/token、块开销 4、角色框架 4）按块类型定价（text/reasoning 计文本、tool-call 计名+参数、tool-result 递归、未知块与 image 引用走保守结构 JSON 价——图片请求价归路由所有）、`estimateHeader` 计 system+tools 非表面信封；**plan/commit 表面折叠**（plan 全部可失败步骤只读执行、commit 原位变异——同一畸形事件每次重试同形失败、绝不半应用；append 落尾、replace 按 start/end seq 定位含闭区间、范围不可解=日志损坏 fail loud）；节点携带持久 image 出现与去图启发价供路由重定价；**路由定价**（无声明价=全表面保持固定启发、provider 中立行为不变；有声明价=图块结构价换成 visual tokens + 模型可见文本价；答出次数≠询问次数 fail loud）；**usage 锚定测量**（锚=最近成功调用的 header+step-start 表面快照+助手输出价+usage；requestHeader 匹配锚 header 才复用（canonical 相等、双方缺席也算匹配），usage 总数 ≥ 该调用的完整路由价锚才用作基线——签发启发式 delta 只能从"不小于全启发价"的锚保守起算、否则整信封重估；`none` 基线=空日志零表面；total 钳非负）；step 括号校验（step/start 嵌套拒绝、step/end 与 assistant/message 必须匹配开启的 turn/step）；usage 锚的助手价从引用 chunk seqs 精确重组 provider 输出（重复/非更早/跨步 seq fail loud、遗留无引用回退 durable 价、显式空表=已知空流 0 价）；Go 适配：WeakMap → 计量器持有 keyed map（键=会话指针）、可选 `ctx.get('llm')` 路由查询 → 构造期 `routePricing` 接缝、事件钩子急观察 → 显式 `Sync`（读时追赶同一 durable 尾）、deepFreeze/structuredClone → 值返回即脱离 |
| `compactionbasic` | `packages/compaction/compaction-basic/src/{types,config,summarizer,region,index}.ts` | 依赖轻量的回放感知 compaction 后端。**配置**：默认策略 0.8 阈值/0.16 保留比、互斥的 retainRatio/retainTokens、summarizationProvider/Model 必须成对（同空同非空）、modelPolicies 精确 provider/model 覆盖表（重复目标 fail loud、每策略独立阈值/保留校验、继承字段逐字段合并）、`TargetPressureConfigError` 可抑制告警的目标级配置失败、ResolveCompactSpec 按适配器 contextWindow 缩放并拒绝 retain ≥ threshold；**summarizer**：逐字固定的结构化 checkpoint 指令作为**最后一条 user 消息**追加在回放对话之后（保留会话自己的 system+tools+消息前缀 → 辅助调用是最近路由请求的真前缀 → provider KV 缓存复用不被失效）、checkpoint preamble/close 标签逐字、finish 映射（max-tokens→MAX_TOKENS）、图片输出拒绝（UNSUPPORTED_CONTENT）、无目标路由逐字报错；**region 事务**：选择/校验只读、空闲校验与 `compaction/start` 同步相邻（durable 开标记即压缩锁）、之后每次失败恰好一次 `compaction/end`（失败入 Error 字段）、关失败故意留下可检测的未配对 start；selectCompactableRange 头锚定 + 保留定价尾巴 + tool 配对平衡（平衡器为驱动组件自有值）、度量面≠会话面 fail loud；stability 两档（whole-surface 全等 / selected-span 同范围同价同平衡、span 外新增节点不失效）、收缩规则=框架化 checkpoint 启发价 < span 路由价（否则 "summary is not smaller" fail loud）、手动分类（busy/commit/changed/summary/persistence/cancelled typed ManualCompactionError）；entry 状态=开 turn/未配对 start/最近 end-seed 三事实倒序一次扫描；**engine**：CompactIfNeeded（pressure=容量解析+阈值+retry 循环；context-overflow=绕过阈值强制一次平衡收缩；pruner 可选独立组合）+ CompactRegion（owner=当前 turn、whole-surface）+ CompactNow（runMaintenance 准入、AbortSignal.any → 链接 context、owner=null+selected-span+flush 接缝、准入失败=busy 维护信号取消=cancelled——源的外层 catch 只包同步准入错误）；RegisterAutomaticCompaction（pre-step 瀑布压力检查+TargetPressureConfigError 告警去重、agent/status idle 清溢出计数、request-error 瀑布 CONTEXT_WINDOW_EXCEEDED 恢复：durable 表面进展即有效重试证据、溢出重试计数按 maxOverflowRetries 封顶）；Go 适配：异步事务 → 同步（summarize 阻塞）、WeakMap → engine keyed maps、`ctx.tokenMeter`/`ctx.llm` → 构造期接缝（Meter/Runtime/ModelInfo/Pruner/Flusher） |
| `jobs` | `packages/jobs/jobs/src/{registry,local}.ts` | 后台任务注册表：`<kind>-N` 单调 id、每属主并发上限（默认 10、负数拒绝）、servesOwner（全局 nil 层服务所有、作用域层沿 ChainOf 匹配）、启动即运行（同步 Run → 原子注册 → 观察协程）、first-wins 结算（record → waiters → notifyChanged → listeners 最后）、Wait 超时返回快照/取消报 `wait aborted`、Kill 先 cancel 后 stopping+reported、teardown 强制失败 `cancel threw during teardown; work may be orphaned`、Dispose 等静止后清空并通知 |
| `toolsjobs` | `packages/jobs/tool-jobs/src/{config,tools,index}.ts` | job 工具面：`background jobs unavailable: no job controller serves this agent (load @deepseek-ai/dsh-tool-jobs in its composition)`、`background job limit reached for this owner (limit: %d); use job_kill to stop an unneeded job, wait for it to finish, then retry`、wait>cap 拒绝、job_output 光标读/终态读与 `[output truncated]`/`[result truncated]`、状态行与完成通知级联裁剪（`[notice truncated]`）、规范值无损 JSON map、system-prompt 段 `tool:jobs`（order 1600）逐字 |
| `skill` / `skillfilesystem` / `toolskill` / `skillbadge` | `packages/skill/{skill,skill-filesystem,tool-skill,skill-badge}/src` | 技能能力组：分层注册表（显式 regScope 注册入层、最近层重名整体胜出、层内 rank→provider 注册序→局部序、`%q is reserved for runtime skill registrations`、runtime 重复注册 first-wins 警告 + no-op disposer、收集缓存 cwd+scope 链+revision 键控、LRU 上限默认 128、`collectCacheMaxEntries must be an integer greater than or equal to 1`、provider List 失败遏制为警告且观察不可缓存、definition 名称漂移→条目失效、`<skill_content>` 渲染含资源提示与 `</skill_resources>` 闭合）；本地文件系统 provider（project(.dsh/.agents)/custom/user(dsh home .system 跳过)/bundled 根序与 100..600 稳定 rank、YAML frontmatter 逐字警告词汇、invoke 策略宽容布尔、松散文件与目录束双形态、缺失文件即缺项、轮询观察器替代 chokidar 同契约：相关漂移失效、maxProjects 界、首次快照为基线不失效、Dispose 等静止）；`skill` 工具 + 会话目录（invalid skill name/unknown or no longer available/not available for model invocation 逐字、可见性匹配——仅本插件精确注册才发布目录、目录消息 catalog 源记录 entries、摘要 sha256 身份、变化原位替换/空目录撤回文案、`/name` 手势仅扫 user 源文本块、手势材料最后注入、pre-step 瀑布 first-registered-outermost 顺序决定背景先材料后）；bundled dsh-badge（嵌入资产、BUNDLED_SKILL_RANK、目录描述逐字） |
| `agentinstructions` | `packages/context/agent-instructions/src` | 工作区指令装载：AGENTS.md/CLAUDE.md 发现（user-global→祖先链→base/local 候选、路径与 trim 内容去重）、字节预算确定性渲染（整体→去最广→截断最具体→紧凑通知、UTF-8 码点边界）、resume 身份校验（一致静默复用/不一致 replacement 宣告+失去 scope 移除）、reconcile 逐 scope 探测（absent→remove、缓存命中静默、trim 重复移除、不可用整组回滚）、`/name` 之外的 desired 折入与 syncInbox 原位替换、tools/result 触碰投影（read/write/edit、parent 归并）；Go 适配：宿主 fs 直连、投影同步化、llm.MessageSource 增 Baseline/BaselineIdentity/Changes |
| `guard` | `packages/guard/{repeat-tool-reminder,timeout-policy}/src/index.ts` | 循环守卫：thresholds fail-loud 校验（nil=默认 [3,5,8]、显式空/重复/<2 拒绝、排序）、`*` 通配 include/exclude（元字符字面）、深键排序 canonicalize（属性序无关）、post-execute 先计数后委托（被拒调用同样计数）、温和/详细提醒折叠到 accept 与 block、previewCap 只约束模型可见文本、用户插话重置链、agent 无键不观测；超时策略：定义 timeoutMs 派生 deadline 换入信号、仅自己计时器触发的 DeadlineExceeded 才替换为 `Error: tool call timed out after Nms` + ToolTimeoutError/TOOL_TIMEOUT、上游取消读作普通取消 |
| `coderuntime` | `packages/code-runtime/code-runtime/src/{index,types}.ts` | 代码执行能力缝（Service Definition 角色，官方不出 provider）：CodeRuntime 接口（Language/Isolation 信息性标识 + Run/Close）、CodeRunRequest/Result（失败是已决结果的字段而 rejection 只报 Service Definition 契约滥用）、六种正交失败类（exception/timeout/abort/worker-exit/invalid-output/output-limit）、绑定命名空间与类型化拒绝类声明、共享校验器（语言可移植标识符子集 + ECMAScript∪Python 保留字并集 + RESERVED_BINDING_GLOBALS 跨后端槽位 + RESERVED_ERROR_MEMBERS/dunder 成员拒绝——官方由每个后端各自执行，Go 把同一校验收进 seam 使"处处一致"结构化） |
| `tmuxcontext` | `packages/context/tmux-context/src/index.ts` | 请求准备期 tmux 位置上下文：单条 tmux/ps 组合查询（`$TMUX_PANE` + 自身 tty 与 `#{pane_tty}` 精确匹配——继承环境读作不在 tmux）、8 格式字段（字面 `\t` 分隔）、step===1 每回合一次、executor 拒绝容器化为警告、稳定状态块变更驱动再注入、`refreshIntervalMs` 只压制变更后的过早再注入、快照源 `{plugin, form:'snapshot', sections}` 前插 |
| `spill` / `spillpolicy` / `spilllocal` | `packages/spill/{spill,spill-policy,spill-local}/src` | 溢出存储接缝（Store.SaveText；locator 不透明、失败必须拒绝）+ post-execute 整形器（nil cap=不注册、负数装载期 fail-loud、先委托再界定、value 替换/block/非文本/read/嵌套直通、通知字节预留进预算、替换永不超 cap、超 cap 保留原文、best-effort 绝不变错误）+ 主机文件系统后端（注入式 `~XXXX` 安全段编码单射可逆、`session-<sha256 12hex>` 目录、0600 独占写 + 0700 私有根、一次性启动清扫：过期正则文件删除、软链不跟随、外来条目不触碰且阻止剪枝、POSIX 属主/祖先信任检查、发现的旧默认根清空自剪、Close 等清扫静止） |
| `checkpointpolicy` | `packages/session/session-checkpoint-policy/src/index.ts` | 语义持久性检查点：llm/stream 首拉前 flush（失败 fail-closed → finish error CHECKPOINT_FAILED、适配器不分发）、顶层工具分发前 flush（嵌套复用外层；flush 失败工具体不执行；成功后中止返回规范 `Error: tool call aborted before dispatch`/AbortError/ABORTED_BEFORE_DISPATCH）、pre-step 请求边界 flush 上一步已提交内容 |
| `sessionlog` | `packages/session/session-log-deepseek/src/index.ts` | DeepSeek 增量会话日志贡献：`AcceptedThrough` 增量折叠（精确会话身份、最高水位、畸形水位 fail-loud 原文报错）、`Prepare` 组装 {version:1, header, afterSeq, throughSeq, 事件后缀}、Accept 持久追加 `delivery-accepted` 水位（crash 于 Accept 前=保守重发）；deepseek 请求扩展注册表（`dsh_session_log` 字段注册：decline 空 sessionId/未知会话/空日志、贡献体 detach 深拷贝、Accept 幂等）；Go 适配：WeakMap → 宿主持有 Folder |
| `toolsjobs/delivery` | `packages/jobs/tool-jobs/src/completion-delivery.ts` | 完成通知投递：job 完成观察（OnJobDoneIn 全局层、`snapshot.Reported` 投递抑制——wait/kill 自见结果不再打扰）、`FitCompletionNotice` 状态行+输出级联裁剪、plugin 源 notice 消息、wake 预算（idle 且预算内 → Followup 计数、忙/超预算 → Inject 静默入箱、无驱动回落 Inbox.Append）、`agent/inbox/claimed` user 源认领返还预算（plugin 认领不返还）、Dispose 断开全部订阅 |
| `hookprotocol` | `packages/hooks/hook-protocol/src/{types,codec,events,matcher,merge,runner,detached}.ts` | 方言无关 hook 执行内核：CommandHook/MatcherGroup/HookOutput 词汇、`ParseHookOutput` 三通道协议（exit 2 → block+stderr 理由；exit 0 且 stdout trim 以 `{` 开头才按结构化解析——top-level `decision` 仅认 approve/block，`hookSpecificOutput.permissionDecision` allow/deny/ask 覆盖、`hookEventName` 不匹配或缺席（有期望名时）丢弃事件级字段但仍记录事件名；其余 exit=非阻断）、matcher 语义（nil/''/'*'=match-all；CC 字面模式 `^[A-Za-z0-9_|]+$` 竖线切分精确匹配；否则非锚定 regex——Go RE2 对 JS 正则的子集差异已记录）、`MergeHookOutputs`（deny/block>ask>approve/allow 排名合并、理由只随获胜名次拼接 `\n\n`、Continue=false 粘滞 first stopReason、additionalContext/systemMessage 按钩子序累积）、`RunHook`（timeout（秒→毫秒，默认 `DEFAULT_HOOK_TIMEOUT_MS=600000`）、JSON payload 经 stdin、CC 追加尾换行 / Codex 不加、`DEFAULT_STDERR_SUMMARY_MAX_CHARS=500` rune 安全截断、事件对 `hook/invoked`+`hook/result` durable 落账 decision 回退链 parsed→stop→pass）、`DetachedRuns`（goroutine+WaitGroup 轨道、Drain 按 cause 取消并等静止）；Go 适配：dsh-shell 进程组缺席 → plain `exec.Command` + watcher goroutine `taskkill /T /F` 树杀（Windows 孙进程持管道孤儿化的实测修复）、shell 解析留 `resolveInvocation` 测试接缝、AbortSignal → context.Context |
| `hooksclaudecode` | `packages/hooks/hooks-claude-code/src/{config,index}.ts` | Claude Code hook 桥：七事件（SessionStart/UserPromptSubmit/Pre/PostToolUse/Stop/SubagentStart/SubagentStop）、settings `{hooks:…}` 包装或裸事件表、非 command 钩子跳过并逐条告警、`${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PROJECT_DIR}` 解析期替换（无变量逐字保留）、matcher 畸形=整份配置拒绝（`<diagnostic> on event "<Event>"` 逐字、读/解析失败仅告警零注册）、camelCase 载荷（`tool_name`/`tool_input`/`tool_use_id`/`tool_response`/`stop_hook_active`）+ `CLAUDE_PROJECT_DIR` env（配置值优先、缺省=会话工作区）+ 尾换行、决策映射（Pre deny→PreDeny+理由 ?? `blocked by PreToolUse hook`、ask→PreAsk、Post deny→PostBlock Feedback+additionalContexts、Stop deny→steer `Inbox.Append(InboxNextStep)`=官方 agent.steer 等价物、SessionStart/SubagentStart detach 注入 `agent.inject` 等价物、上下文折叠先委托后追加保序、handler id `claude-code:<point>:N` invoked/result 配对）、subagent 子代理表按 runId 配对保留至 end 释放；Go 适配：subagent 生命周期在 registry 总线上订阅（组合层把同一总线交给 subagent runtime）、transcript_path 走可选 `LocateTranscript` 接缝（nil=官方无持久化服务时的 `""`）、`runHook` 变量测试接缝 |
| `hookscodex` | `packages/hooks/hooks-codex/src/{config,index}.ts` | Codex hook 桥：五事件（无 subagent 点）、`async:true` 与不支持类型跳过告警、`timeout`/`timeoutSec` 别名、matcher 一律正则（无 CC 字面快速路径）、snake_case 载荷（`tool_input` 恒 `{command: <call 的 command 参数>}`、`turn_id` 字符串化于回合事件、Stop 载荷带 `stop_hook_active:false`+`last_assistant_message:null`、SessionStart 带 source 无 turn_id）+ 每载荷 `model`/`permission_mode:'default'`、无 env 无尾换行、仅认阻断决策（allow/ask 落空）、plainStdoutAsContext（SessionStart/UserPromptSubmit/Stop：exit 0 且无结构化上下文且 stdout 非空非 `{` 开头 → 整段作为 additionalContext——raw JSON 永不泄漏为散文）、handler id `codex:<point>:N`；Go 适配与 CC 桥同构（registry 总线、LocateTranscript、runHook 接缝） |
| `schedule` | `packages/schedule/schedule/src/{types,domain,runtime,tools,transaction,index,invariant}.ts` | 会话本地持久提醒：版本化 `schedule/change` 流为唯一持久态（v1 严格解码——精确键、canonical 四位数年 UTC 瞬时、已裁剪 prompt、everySeconds≥300 安全整数、id 非空无空白；RE2 无前瞻 → 0000 年代码层排除）、fold 派生活跃集（创建序保留、dispatch 精确 retire/advance、fork 只折叠 seedLength 后缀、复用 id/失效目标/one-shot 带 acceptedAt 全 fail loud）、纯规则数学（创建对齐 every、`ResolveEveryOccurrence` 不枚举积压一步取最新出现+耗尽判定、explicit-offset 解析拒绝 -00:00、本地 at 经 time.LoadLocation+五采样偏移投影——重叠取最早、间隙拒绝；IANA 规范别名不折叠与 tzdata 内嵌已记录）、防注入固定 framing（单发/every 批量、prompt 全 JSON 转义）；三工具 schedule_create/list/delete（恰好一个 selector、闭集错误码、cross-scope → internal_error、corrupt log → corrupt_schedule_log、flush 失败 → persistence_uncertain 携带 operation(+id) 绝不从活日志推断）；`ScheduleRuntime` 单代理一次性投影（触发合并串行排空、精确 root 身份 liveness、preflight flush→fold→分段定时（MAX_TIMER_DELAY_MS 钳制）、到期 RunMaintenance 认领→重查钟与身份→完整 framing 先于 Followup→enqueue 返回后才追加 dispatch（append 失败 faulted 闩+告警、模型失败不回滚）→barrier flush；忙 → waitForIdle）；plugin 只挂未来活跃 root（created+roots 校验、idle 且日志含 schedule/change 才驱动）；Go 适配：ctx.sessions.flush → 构造期 FlushSession 接缝（nil=检查点平凡完成）、WeakMap 事务尾链 → 指针 keyed map+channel 前驱、invariant 伴随件缺席（Go 无 invariants runtime，fold 边界校验覆盖同一流）已记录 |
| `sessionquery` | `packages/session-query/session-query/src/{config,cursor,corpus,documents,extraction,filters,index,observation,sources,tracing,types}.ts` | 会话逻辑语料库读取面：live-preferred corpus（live 命中即返回不触持久化；persisted 补充；live/persisted 头冲突 SOURCE_CONFLICT 不合并；createdAt DESC + id 字节序平局）、`ProjectMany` 有界并发批投影（输入序输出、单会话失败隔离在 Reason、caller 取消 → ABORTED）；一次性 foldSurface 分类 current/shadowed/log-only（INVALID_SURFACE）+ 全 detach（替换链沿 replacedBy 单向、sourceEventSeqs 不传递）、祖先链+后代树 trace（环 → INVALID_LINEAGE、缺失父 → Complete:false+UnresolvedParentID、目标缺失 → SESSION_NOT_FOUND）；filters AND+子句内 OR、空列表不匹配、含端点范围（NaN/Inf/from>to、未知 kind/surface/availability → INVALID_FILTER）、text=字面大小写不敏感空白灵活扫描（regex 元字符字面、RE2 无前瞻不适用）；extraction 每事件类型语义文本（user/assistant 消息块、tool-call 名+参数、tool-result 内容+错误名/码、todo 状态+content、turn/end 理由、结构事件空串）；title=最后 `session/title` 折叠（fallback|provider{provider,model?}|user、messageSeqs detach、normalize 剥 OSC/CSI/C1/控制/方向字符+空白折叠+UTF-8 字节预算不劈码点、collect 只取 user 源非空白 user/message）；读事件窗口 before/after 0..readWindowMax（默认 50）钳制、SearchBackend 缺席 → SEARCH_DISABLED；`SessionObservation` live 即时/prepared 借出（Revision 绑定、Retain/Release 引用计数、Release 幂等）；17 常量错误码闭集 `SessionQueryError`；Go 适配：localeCompare → 字节序、Range `*float64` 保留 number 校验面、reader 的 SOURCE_CONFLICT 分支为防御（coordinator 先行拒绝身份不匹配）、session-title 纯 fold 内嵌（SessionTitleService 随宿主轮 deferred）、sqlite/tool-session-query/session-log-export 兄弟包不移植 |
| `preset` | `packages/preset/agent-presets/src/{preset,types,specifier,metadata,discovery,authoring,mount,index,session,invariant}.ts` + `packages/preset/persona/src` | agent 预设组：词汇（TrustSystem/TrustUser、id 规则 `/^[a-z0-9][a-z0-9-]*$/`——目录名即 id 否则可逃逸根、`agent.cordis.yml` 组合、`preset.yml` 显示元数据、`.agent-presets` 可写根）、错误闭集逐字（`preset "x" not found (available: …|none)`、locked `session "s" has already started; its agent preset is fixed`、`failed to mount: <reason>`、`preset id %q must match …`、`already exists — a copy never overwrites…`、`cannot be written: <reason>` 三 reason——无用户根/随部署发布/不在可写根下）、metadata 读写（name/description text() trim 空即 undefined、order 有限数、读失败全降级 `{}` 不致命、render 省略空字段全空不写）、row specifier 分类（`cordis:`→builtin、`.`→preset、`file:`/绝对路径→file、其余→package）、discovery 健康（shape 检查：顶层行列表、行=带 name 的 map、group 递归、缺组合文件的目录仍占 id→broken 逐字、包解析=node_modules 向上 walk+@scope 两段+subpath、file/preset stat isFile、disabled 行跳过——falsy 值仍算启动）、first-root-wins per id、order 升序 nil→+Inf 平局 id、scanRoot 不存在→空其余 fail loud、authoring（copy=整目录拷贝+POSIX tighten 0700/0600/owner-exec 保+metadata 重写保 description 删 name/order+失败 rm -rf 重抛+永不覆盖、delete 拒非 user 与越界路径、live 会话挂载不拒——组合创建时已读绝不重读）、roster（resolve、settings 覆盖层读默认、remove 清相等默认、read/copy/remove、mtimeMs+size stamp）、`agent-preset/selected` 事件+`agentPreset` 投影（init=header、selection 覆盖、空串=显式清除、schema string|null、stateVersion 1）、persona 行（scope-only——根 scope 与部署注册冲突、complete 旗标、includeRuntimeContext=false 抑制）；Go 适配：localeCompare→字节序、isBuiltin 缺席（package 仅磁盘 walk）、pathToFileURL/drive-letter→绝对路径直用、`!!js`→`!!str` 预处理（truthy 语义近似）、standing mounts/scope reparent/recompose/serviceForAgent/泄漏审计、Typert remote、settings 真服务→接缝（DefaultOverride/ClearDefault/ShippedRoot/Getenv）、persona 以 systemprompt 直接调用实现、invariant 伴随件缺席（Go 无 invariants runtime）已记录 |

**当前门禁状态：68 包全绿，956 个行为测试，`gofmt`/`go build`/`go vet` 干净。**

**已记录的延迟项**：DeepSeek 图片路径（Files API/文件商店/请求定价/offload 策略——需 attachment 服务，文本路径对 image 块一律 UNSUPPORTED_CONTENT，与官方无 attachment 服务时行为一致）；llm content.ts 图片投影；zstd 压缩变体；tools 的 PTC `run_code` 传输与 presentation 渲染器（`WireSchemas` 非 native 模式即 seam 处 fail loud，官方 `requireCodeTransport` 语义）；settings 节安装与 `agent-loop/config-start-failed` cordis 事件接线（随 settings/boot 组装轮补，当前配置启动失败走 logger 告警）；sessions 注册表 `enter/announce`（Go 侧 publication 仅进 agent registry，已记录偏差）；preset 的 cordis 专属面（standing mounts/scope reparent/recompose/serviceForAgent/泄漏审计——Loader 机制，Go 编程式组装无 cordis fiber；Typert remote；settings 真服务→接缝 DefaultOverride/ClearDefault/ShippedRoot/Getenv 已留）。

## 语义决策记录（每条可追溯到官方）

- **workflow 脚本执行域 = Go 原生函数域，替代官方 worker-thread plain-JS realm**（R10）：官方第三方 workflow 脚本是 JS 源文本，跑在 worker 线程 JS realm（宿主求值隔离 + 超时可杀）；Go 侧对应物是编译期装配的 Go 脚本函数（`StartRequest.Program`），引擎宿主契约（ScriptAPI 六件、事件词表、配对/settle/fatal 纪律、invariant 观察）与官方逐语义等价，事件 wire 不变。**兼容边界（明示）**：官方 JS 脚本源文本在 Go 域不可执行——`Program` 即 Go 侧脚本形态；官方"meta 校验绝不求值脚本文本"的隔离理由在 Go 侧同样成立（plain JSON meta 与 Go 函数域天然分离）。来源：`packages/workflow/workflow-worker-thread` 宿主契约 + dsh-go `workflow/engine.go`。

- **agent 事件总线类型化访问器（`TypedWaterfall[T,R]`，样板=agent/pre-step）**：Go 无泛型方法，故以值类型句柄（`Events().PreStep().On/Dispatch`）绑定载荷/结果类型，any 断言只存在于类型边界一处（构造保证其成立）；监听器表、作用域准入、first-registered-outermost 次序、"next 恰好一次"契约全部不变——同一张 any 表驱动，raw `OnWaterfall` 仍可用于未迁移事件（同一事件名不得混用两种访问器）。迁移只是消灭消费方的 assert-and-decode 仪式，非行为变更。五个 waterfall 事件已全部接入：`PreStep()`、`Request()`（链上为 `*llm.LlmCallConfig` 指针流——擦除时代 base 值/监听器指针混流的隐性不一致借此显式化并统一）、`RequestError()`、`userquestions.Requests()`、`userapproval.Approvals()`。
- **interaction 两 waterfall 的联合类型 seam 收敛**：user-questions 的 R 联合（`Answer|*Answer|UserQuestionError|*UserQuestionError|error`）收敛为单一具体决策类型 `QuestionDecision{Answer, Err}`——不可识别返回形状的运行时归一分支被编译期排除（原"foreign shape→NoProvider 归一"的 fail-closed 语义上移为类型边界保证；answerer panic 仍由 dispatch 的 recover 含容为错误）。userapproval 的 R=词表字符串保留，但类型经 seam 闭合；**词表值归一保留**（typed 仍可能携带任意字符串值，`outcomes[decided]` 检查照旧归一 Unavailable），panic 含容改为 `containedDispatch`（recover→fail-closed base，语义不变）。事件名、payload、作用域准入、组合次序零变化。
- **委派策略落盘闭合（`sandbox/mode` 接缝接通）**：child-agent 轮预留的"captured override 仅内存"接缝闭合——`permissionpresets.Service.OverrideOf` 实现了 `subagent.SandboxOverrideService`（只读会话显式 `sandbox/mode` 折叠，绝不回退部署默认），`AppendDelegatedPolicyOverrides` 在 approval pin 之外追加 `sandbox/mode` pin（typed 词汇、create 分支、未发布创建窗口内），子日志仅含本构建可折叠的类型——冷恢复从日志重建同款委派策略，不再依赖内存窗口。
- **emit/serial 总线类型化（`TypedEmit[T]`/`TypedSerial[T,V]`）**：fire-and-forget 与串行链同一边界规则——载荷断言收拢到句柄一处，按事件 accessor（agent 包方法 `Created/Status/SessionStart/InboxClaimed/InboxDiscarded/TurnStopping`；subagent 载荷类型对 agent 不可见，`subagent.Starts/Ends(bus)` 包级构造）。生产 11 处 emit + 2 处 serial 监听器全部迁移，raw `OnEmit/OnSerial` 保留给测试与一次性用途（同一事件名不得混用）。serial 的 bail 值类型在生产中从未非 nil（hooks 边界只经 inbox steering），`TurnStopping` 的 V 暂为 any——诚实形态，非留白。**迁移暴露并修复一处潜伏缺陷**：continuation manager 的 accepted-id 排水监听器用共享擦除解码器只匹配 discard 载荷形状，而 claimed 事件携带不同 struct——"claim 或 discard 排水"文档语义中 claim 一半是死路径；分型强制两个载荷类型拆开后修复，回归测试钉住两条边。
- **投影单元泛型化（`projection.Unit[S]` + 显式 changed 门）**：Go 泛型类型+擦除构造器（`Unit[S].Definition()`）替代全 `any` 状态与"同引用=无变化"的隐式门——`Apply` 返回 `(S, bool)`，false 显式透传旧状态（注册表引用快路径逐位保留），true 才发布。any 断言只存在于 `Definition()` 类型边界一处（构造保证成立：单元状态只出自同单元 Init/Apply/DecodeState）。"新分配但未变化的状态谎报变更"这一类静默 bug 被编译器消灭。六个单元（sessionstats/subagent×2/todo/planmode/agentPreset/permissions）全部迁移；`permissions` 的共享 fold 在单元边界以可比较结构体等价计算 changed。客户端 JSON 值逐字不变（agentPreset 的 null/string 由 View 展开保证）。
- **cordis 服务键类型化（`cordis.Service[T]`）**：`DefineService[T](name)` 把服务名绑定到 Go 类型，any 断言收拢到句柄一处——`From(ctx)` 沿祖先链解析（absent/nil ctx/错误类型一律 `ok=false` 降级，不 panic），`Provide` 对称发布。已接线：`agent.ContextService`（"agent"，factory 发布/子代理创建窗口消费）、`webserver.ContextService`（"webServer"）。tools 注册表的 `Get(name, scope)` 是键控工具表不是服务定位器，不在此列。
- **WeakMap→`weak.Pointer` 评估结论：不采用**（如采用需 go directive 1.23→1.24）。调查 37 处生产站点（14 文件）：全部要么带显式 delete/dispose 钩子（projection cells、continuation、controller pending、coordinator live、engine overflow*、transaction byAgent…），要么与宿主同生命周期（meter states、sessionlog folds）。Go 的显式确定性清理**严格优于** JS WeakMap 的 GC 时机不确定——原 TS 痛点在 Go 移植中已被更idiom的机制消除；引入 weak 反而把确定性换成 GC 定时。重审条件：出现全局长寿命注册表以会话对象为键且无淘汰钩子的泄漏面（当前未发现）。
- **会话词汇注册收拢到装配层（②）**：12 个域包的 `init()` 副作用注册（含 panic 路径与"导入即注册"）改为各包显式 `RegisterEvents()`，由 `boot.RegisterVocabulary()`（`Assemble` 第一步）统一调用——静态构建的"插件加载时刻"就是装配，词汇成员资格由这一处显式决定。注册走幂等的 `session.EnsureEventTypes`（重复装配安全）；严格版 `RegisterEventType` 保留给真插件合并语义与冲突测试。词汇门只在持久读路径咨询（persistence coordinator），内存 append 不设门——因此契约是：**一切入口必须经 `boot.Assemble`（或先调 `RegisterVocabulary`）**，越过装配面读日志按 fail-closed 拒绝（拒绝而非腐化，安全方向）。
- **Get 仅沿祖先链**（self → parents）；Waterfall 必须调 `next()`；Effect 在 Dispose 之后注册立即执行 disposer；Dispose 收敛所有失败。来源：cordis-primer + postmortem 0001。
- **patch override 是整字段替换**（`target[key] = value`，无合并）；patch 未命中目标只告警不报错；插入行被索引供同一列表中后续 patch 命中。来源：`vendor/include` `applyEntryPatches`。
- **`!!js` 同时匹配速记 `!!js` 与完整 `tag:yaml.org,2002:js`**：yaml.v3 对未知速记保留字面 tag，与 js-yaml 的解析产出不同，两种形态都要接受。
- **settings 深相等写入不发事件、不增修订**：对齐官方 deep-equality 规则，同时杜绝 save→reload 回环。
- **watcher 在存储锁释放后派发**：持久化 watcher 回写文档需要再拿锁——持锁派发会死锁（已在测试中暴露并修复）。
- **credentials 记录的 scope 是属主插件名**（不是域），载荷按属主格式写，`/` 保证两个键空间文法不相交。
- **session 事件 Data 用 `json.RawMessage`**：词表可扩展且逐字节兼容官方日志；已知类型提供类型化解码器，未知类型拒绝整份日志（fail-closed，加普通事件类型不 bump 格式版本）。
- **workspace 实体 no-op 门 = 引用相等**（对齐官方 `changed === current`）：Go 值语义无法携带引用身份，`errNoChange` 哨兵替代"fn 原样返回输入"；`SetTitle` 恒构造新记录 → 同值也落盘并刷新 `updatedAt`（官方 `{ ...record, title }` 行为），幂等路径（attach 已计入 / detach 缺席 / 移到原位）保持无写入。
- **timecontext 浏览器时区投毒走 `PreStepReject`**（官方沿 waterfall 抛错 → error 终结）：Go 同步 `OnWaterfall` 接缝无错误通道，拒绝是终结语义下最接近的务实选择——终结词汇可观察不同，已记录为适配。
- **planmode `Set` 的 durable append 失败返回 error 给调用方**（官方 append 抛异常传播）：不再吞成 `queued`；pending 选择保留可重试（delete 只在成功后），与官方 `pendingIntents` 生命周期一致。
- **storagedomain 监听器锁外派发**（对齐官方单线程 emit 的同步重入能力）：写路径 mu 内完成 backend+内存提交并登记队列，派发在 mu 释放后按提交序进行；监听器可同步读快照甚至再入队写（嵌套写事件 rides 同一队列，仍保提交序）。Go 适配：并发写者下，派发中读内存可能看到更晚提交的值（官方内联 emit 不会交错）；事件载荷始终是提交时值。
- **TS `??` → Go 映射约定**（全局排查结论）：`??` 仅对 `undefined`/`null` 回退、不捕空串——Go 侧按操作数形态选映射：可选标量用指针/`(value, ok)` 元组（如 permissionpresets 的 `EffectiveSandboxMode`）、map 读取用 ok-check、`string` 一律以 `""` ↔ `undefined`（空串即"未传"）。特例：workspace `Create(path, title)` 的空标题即"未传"→ basename（官方 `title ?? basename`；官方无调用方显式传 `""`，行为无损——R8 决策）；plan 投影 view 的 `running?.wanted ?? wanted` 用指针逐字对齐；未来 exit 工具 port 时 `firstHeading(args.plan) ?? 'Plan'` 须按 `""`→`'Plan'` 映射。
- **subagent 生命周期边经 registry 事件总线派发**（官方 `ctx.events.dispatch('emit', [carrier, name, info])` 自带逐监听遏制+logger 告警）：Go `SubjectEventBus.Emit` 契约已含逐监听遏制，运行时按 parent 的 ScopeKey 作 dispatch carrier（provider removal 无载体→nil 全局）；start/end 以**同一次铸造的 runId** 配对（identity 只建一次），start 同步先发后启终局 goroutine 复刻 Promise 反应时序；`EpochStopReason` 的 aborted-vs-error 优先序：recorded failure 胜过 droppedUnrun 取消——"停掉一个已经失败的孩子不把失败改写成取消"。

## 锁定的 wire 契约（改动 = 破坏第三方插件）

- 路由 kind 词汇表：`exact` / `prefix` / `fallback`；服务键 `webServer`、`settings`。
- llm 消息/内容块/流块 JSON 字段名（camelCase，与官方日志逐字兼容）。
- `SessionEventMap` 事件词表、SessionHeader 字段、SurfaceOp 双形态。
- settings 文档 = 命名空间 → 段 YAML/JSON 映射。

## 构建

工具链：便携版 go1.27.0 @ `D:\workspace\toolchain\go`（SHA256 已对官方 go.dev 清单校验）；GOPROXY 已配置 goproxy.cn 优先。

```powershell
$env:PATH = "D:\workspace\toolchain\go\bin;$env:PATH"
Set-Location D:\workspace\dsh-go
go build ./...
go vet ./...
go test ./... -count=1
```

注意：`-race` 需要 cgo（本机无 C 编译器），测试不带 `-race` 运行。

## 后续路线（按依赖序）

1. ~~`tools` 注册表与执行管线~~（已完成；PTC `run_code` 传输 + presentation 渲染器随 SDK/PTC 轮补）
1. ~~`systemprompt`~~（已完成；cordis 接缝——注册经 `agent.ctx` 派生 scope——随 agent 轮补）
1. ~~`agent`（registry/inbox/consumed-work/dispatch/model-selection）~~（已完成；typert 查找注册随 typert 轮补）
2. ~~`agent-loop`：turn/step 驱动机~~（核心完成：驱动、步进、请求记账、工具调度、runtime-context、服务层/工厂；settings 节安装接缝随 settings 组装轮补）
2. ~~`interaction` 审批/提问/ask-user 接缝~~（已完成：userapproval + userquestions + toolaskuser）、~~`interaction/commands`~~（已完成：注册表/ParseCommand/lifecycle 事件/图片准入门/change 通知；UI dispatch 适配器随 webserver 轮）、~~`interaction/permission-presets`~~（已完成：服务/投影/`/permission` 命令定义；settings section 与 `session/created` 钩子随装配轮）、~~`plan` 模式 exit 工具与 `/plan` 命令~~（commands 注册表已就位——随 user-questions 组装轮接线）；permission-presets 完成
2. ~~`subagent` 基础层+运行时+组装层~~（已完成：depth/descriptor/assistant-output/run-settlement/activation-setup-registry/SubagentRuntime 注册表+Start 能力门+生命周期 start-end 配对/EpochStopReason/ActivationObserver/child-agent 组装全量（深度预算/元数据/options 解析/组装合成/委派策略）/continuation 类型面+ContinuationManager 接缝+manager 核心切片（ChildLock/stateOf/授权/中断/closing scopes）；manager 本体（materialize/create-resume/submit/watchSettlement/drain）、list-children/ACP provider 随后续轮补）
2. ~~`workflow` 接缝层 + 脚本执行引擎~~（已完成：词汇/typed fatal errors/事件词表/EventSink/meta 校验/engine（Go 原生脚本域替代 worker-thread JS realm——决策见"语义决策记录"，官方 JS 脚本源文本在 Go 域不执行）+ invariant 观察器）
2. ~~`todo`~~（已完成：todo_write 工具 + todos 投影单元；client 呈现面随 SDK/web 轮补）
2. ~~`homepaths`/`identity`/`webhook` 规则运行时~~（已完成：home 解析 + 匿名 id + fire-and-forget 规则运行时；webhook 的 SessionCreator 服务组合随 workspace/preset 轮接入，GitHub 适配器随需要时补）
2. ~~`compaction` 接缝层~~（已完成：词汇/事件/checkpoint/tool-pairing）、~~`token-meter` 计量核心 + O(1) 投影单元~~（已完成：估计器/表面折叠/路由定价/usage 锚定测量；**投影面已落地**——FoldSurfaceTokens 影子价 O(1) 折叠（compaction/summary·prune 武装 claim、相邻 replace 消费、无 claim 中性、错位 fail-loud）+ tokenUsage/contextPressure/contextBreakdown 三个投影单元（usage 单槽替换样本+retry-started 关槽、pressure 独立 last-wins 槽+projectedTokens=max(0,采样+表面位移)、breakdown 信封 last-wins+消息走同一折叠；状态固定少数几个数，检查点 O(1)）+ **turn-usage 折叠**（attempt 状态机 idle/open/finishClosed/settled、error/aborted finish 关闭、llm/retry 结算+retry-started 同步重开、聚合可选桶全在才在、路由首次出现序去重、任一缺失 fail-closed 返回 nil）；~~`compaction-basic` provider~~（已完成：config/summarizer/region 事务/engine 四触发器；compaction-pruner（tool-result-pruner 模型无关修剪）与 `/compact` 命令接线随后轮）
2. ~~`workspace`~~（实体层 + registry 引导/create-delete 事务全部完成；webhook SessionCreator 事务句柄已就位）、~~`context` 插件 time-context~~（已完成）、~~`plan` 模式核心~~（已完成：折叠/Set 四态/pre-step/plan:policy 节/plan 投影；exit 工具与 `/plan` 命令随 user-questions/commands 轮）、~~`storage-domain` 域运行时~~（已完成：规格/域运行时/facility/MemoryUnit）、~~`storage-json` 文件后端~~（已完成：atomic 发布/format 校验/single+per-record 单元/legacy 引导/backend）、~~storage hub~~（已完成：backend 注册表/hub 表单/服务键；装配轮接入 loader）；`context` 其余插件（tmux/git）、storage-sqlite（SQLite 后端，与 JSON 后端同契约）
3. ~~`llm` 运行时 + DeepSeek provider~~（已完成；llm-deepseek 插件装配层——settings section 接线、credential 解析钩子、retryPolicy 变更时 Replace 重注册——随 settings/credentials 组装轮补）
4. ~~`tools` + `system-prompt` + `agent` + `agent-loop`（request/header 记录、turn/step 括号、中断收尾、waterfall 请求改写）~~（已完成）
5. ~~`interaction`（approval/ask-user）、`subagent`/`workflow`、`workspace`、`identity`、`compaction`（surface replace 消费者）、`context` 插件（time-context、tmux）、`webhook` 规则运行时~~（已完成；tmuxcontext 为完整移植：Query/Render/ValidateConfig）
6. ~~`sdk` JSON-RPC 服务器 + `boot/app-boot`（profile 装配、patchReload、fail-loud）~~（已完成：sdk/server+client+protocol；boot/profile.go 396 行含 ProfileTemplate/InitProfile/patchReload 全语义）；~~`settings`/`settings/file`/`credentials`/`llm-deepseek`~~（包已交付：Store/PathOp/file 后端/Provider 接缝/deepseek 插件+credentials 解析）

**剩余（按依赖序，2026-08-29 账实对齐后）**：
1. **插件目录与顶层组合（关键路径——目录与顶层组合均已落地，余量在插件覆盖）**：`boot/catalog.go` 目录基建（`NewCatalog(deps)` 官方 npm 说明符→Go 组合 spec，缺名 fail-loud）+ `boot/appboot.go` 顶层组合（`AssembleProfile`：LoadProfile 层序→ComposeEntries→Assemble，`App.Root()` 服务访问）+ **`cmd/dsh` 可运行入口**（-profile/-home/-anchor/-list，实跑验证：模板 profile 自动初始化→bundle 解析→patch 组合→catalog mount→服务表）。已接线 **43/86**（批次 1-2-3：tools/commands/settings-file/credentials-local/web/session/session-projection/agent/llm/llm-deepseek（deepseek.Apply 装配层）/session-persistence-jsonl（Coordinator+storeadapter.go 接缝）/user-questions/user-approval/permission-presets（服务+settings section+session/created 钩子全量——`permission` settings 区驱动新会话 defaultPreset（Scope.Get 解析 defaults→base→user 即官方 setSource）、OnCreated 可否决钩子逐会话 PinInitialPermission、组合期对存量会话回填；Inject sessions 为声明依赖；组装测：ProviderPush 切换用户区→新会话即随新值）/system-prompt/agent-loop/**subagent（ManagerExt 生产装配**）/**spawn+fork-in-process provider**（`subagent/inprocess.go` 一次性子代理全生命周期驱动；outputSchema 能力位如实 false）/skill+tool-skill+tool-todo+jobs-local+tool-jobs+plan-mode（/plan+exit 工具双注册）+repeat-tool-reminder（Effect 挂双 detach）/**token-meter**（单例重放感知计量器，图片路由定价缝缺省 nil 走估算；Inject projections 注册 tokenUsage/contextPressure/contextBreakdown 三个 O(1) 投影单元——units 见条目"未移植面"上方的 compaction/token-meter 行）/**compaction-tool-result-pruner**（`toolresultpruner` 包：模型无关 head/marker/tail 码点预算剪枝——只处理 content[0] 内嵌 content、标记恰好一次、富块保序、replacement 必须更小且入阈 fail-loud；PruneSession 快照当前 surface 一次防自食、compaction/prune 影子价事件与替换相邻落地、替换保 message id 只换内嵌 content；配置 DecodeConfig 未知键拒绝 + head+marker+tail≤threshold fail-loud；组合纪律：须挂载在 compaction-basic 条目之前（cordis apply 序，与官方 README 一致），缺位即引擎无剪枝独立可用；engine 级集成测：压剪后清压→跳过摘要）/**compaction-basic**（Engine：摘要走 llm runtime、容量走同 runtime 的 model info、落盘走 Coordinator.FlushSession，配置组合期 fail-loud）/**command-compact**（/compact——compactStarter 契约改为携带 Invocation：引擎 CompactNow 的 MaintenanceAgent 由 invocation.Agent 经 maintenanceOwner 适配器绑定，driver.RunMaintenance 承载保留轮）/**tool-subagent-control**（`subagentcontrol` 包：send_message/interrupt_agent 走 runtime Followup/Interrupt（authority=ancestor），list_agents 走 ListChildren/ListDescendants 的 continuable 投影折叠——冷读 queryEngine 经 Coordinator.Load+NewRestored+投影切面，status 三态 running/open-turn 切面、idle/驻留、ready/仅持久化；一次性子代不进发现面）/**shell 域五件**（shell-env/bash-local/pwsh-local/tool-bash/tool-pwsh，见条目 8/9/10））。待续：其余 42 项（webhook、api/gateway Remote 网关面——原"webserver UI dispatch 适配器"的精确残余（commands Execute 面已在，网关经 typert lookups 解析 agentId 参数；host/webserver 路由/升级/index 注入已落地，gzip 缓）、storage-sqlite、ACP、PTC、结构化捕获轮、web 家族/session-title/checkpoint/spill 等重件），profile patchReload watch/live 后半轮，插件管理 CLI（`dsh plugin --profile … add`）随 CLI 轮。**账实对齐 2026-08-29**：官方树无 git context 插件（无包、无 npm 名，README 旧尾巴已清除）；typert 组 = 构建期生成器 + 运行时 registry + loader 集成 + Remote 协议——Go 侧 registry 的有意义消费方仅 api/gateway+remotes，registry 本体与网关同轮按需落地，不预造无调用者服务。**webhook SessionCreator 受阻盘点（2026-08-29，如实记录不硬凑）**：官方 createWebhookSession 事务（packages/webhook/src/session.ts）依赖四项 Go 侧未移植缝——① agentPresets.mount（preset cordis.yml 组合挂载进 agent 作用域 ctx，Go Roster 仅 Resolve/ResolveMountable 无 mount）② session-title（sessionTitle.rename，Go 无 title 包）③ agent-default-model（ctx.agentDefaultModel.currentSelection() 兜底选路）④ standingKeyFor（preset 的持久压缩键）；待四缝按依赖序落地后组合 webhook 条目（webhook 规则运行时本体+SessionCreator 缝已就位）。
2. **未移植面**：storage-sqlite（与 JSON 后端同契约）、ACP provider、api/gateway Remote 网关面（typert registry 与 remotes 随该轮）、PTC `run_code` 传输与执行基底（**受阻盘点 2026-08-29**：`coderuntime` 缝已落地；传输面 ptc.ts 依赖 CodeRuntime 后端——官方 TS 后端是 node worker_threads + 原生类型剥离，本机无 node、Go 侧亦无进程内 JS 基底决策，待基底轮定夺后传输/SDK section/后端按依赖序跟进；Python 后端协议同轮评估）。（subagent list-children 已随 `subagentcontrol` 的 list_agents 落地；token-meter O(1) 投影单元与 turn-usage 折叠已落地——llm-retry 插件未移植，`llm/retry(-started)` 词汇仅作只读折叠预留；compaction-pruner 已随 `toolresultpruner` 落地——Engine 可选缝已接线；context tmux 已随 `tmuxcontext` 落地；git context 官方无此物——账实对齐清除。）
3. **文件工具家族依赖根已移植**：`fs`（官方 dsh-fs seam 全量词汇：不透明 TargetKey/Version 身份、Observation/Info/PathInfo/DirEntry、WriteIntent 守卫（createIfAbsent→FS_NOT_OBSERVED / replaceIfVersion→FS_STALE_VERSION）、Write/EditOutcome 的 before/after LF 规范化 diff 基、13 个 FS_* 稳定错误码 + FsError 类型、fs/write-intent·edit-intent·observed 三个 cordis 事件名、FileSystem 接口含 sandboxMode 能力位与 per-call SandboxExecutionPolicy 缝）+ `fslocal`（本地后端：realpath 身份+缺失后缀祖先行走键稳定、NUL+UTF-8 二进制拒绝、64KiB 流式解码含拆分 rune 回退、ReadBytes 上限在打开描述符上执行 FS_TOO_LARGE、稳定名序 ListDir、写/改 per-key 锁串行化读→守卫→写窗口、editText 版本守卫先于字面匹配、old_string 唯一性纪律（0→EDIT_NOT_FOUND / >1 无 replace_all→AMBIGUOUS_EDIT）、原子发布同目录 temp+rename 保模式；诚实降级：可移植 os.FileInfo 无 dev/ino/creation，version token 退化为 size+mtime——token 不透明故对消费者不可见）。官方 86 名单无裸 dsh-fs/dsh-fs-local 条目——catalog 接线随 dsh-fs-sandbox/dsh-fs-observation-policy 轮；tool-str-replace-editor（528 行）**已在其上落地**（`strreplaceeditor` 包：view（cat -n 行号渲染+view_range [start,-1] 纪律+目录两级树排除 dotfiles/node_modules/__pycache__）、create（已存在即 plain 拒绝）、str_replace（歧义报 FS_AMBIGUOUS_EDIT 且列出全部命中行号、未命中报 FS_EDIT_NOT_FOUND、json-null new_str 拒绝）、insert（insert_line∈[0,lines]）；写路径经 fs/write-intent·fs/edit-intent 单槽 waterfall 决策 + fs/observed 观察记录（Go 适配：三事件均走 ctx.Waterfall）；绝对路径纪律带修正提示；MutationPolicy 语义保留——backend 不设 sandboxMode 时 policy 可缺位，设了而服务缺位则组合 bug fail-loud）+ catalog 条目 tool-str-replace-editor（Inject tools/fs/agents；fs 服务由 fs-sandbox 轮提供，缺位即 inject 期 fail-loud——组合纪律而非缺口）；tool-fs（1416）/tool-fs-search（1486）在此地基上分期。
4. **沙箱策略与围栏（官方 dsh-sandbox/roots + dsh-sandbox-policy + dsh-fs-sandbox 三家）**：`sandbox` 包（可写根推导的唯一家——workspace-write = 工作区根 + /tmp + 用户平台临时目录，canonical 化+去重；词法包含快路径 Windows 大小写不敏感、目录边界前缀不误配）+ `sandboxpolicy` 包（部署默认 mode+回退工作区根的唯一所有者；解析优先级 approved > session 最后 sandbox/mode 覆盖 > 部署默认，session cwd 即 workspace-write 边界、缺位落配置根；空 mode fail-safe 为 read-only；Go 适配：Resolve 取显式输入——session cwd/覆盖/批准 mode 三参，executor 无会话态；sandbox/mode 事件词汇与折叠除本就在 permissionpresets，策略服务消费之）+ `fssandbox` 包（本地后端全量机制原样承袭、仅对两个写操作加 per-call policy 围栏：read-only 拒绝、workspace-write **当场再 canonical 化**并要求可写根包含（捕捉工具解析后换掉的符号链接祖先；TOCTOU 残留如实按威胁模型接受）、danger-full-access 不设防；包含检查=词法快路径+文件系统身份回退（os.SameFile 祖先行走，认 Windows 8.3 别名与大小写）；拒绝抛 FS_SANDBOX_DENIED 带模式文案）。catalog +2（累计 34）：dsh-sandbox-policy（Provide sandboxPolicy）、dsh-fs-sandbox（Inject sandboxPolicy、Provide fs——装它而非裸 local 即全量换装，模型侧工具不动）；tool-str-replace-editor 条目按需挂 mutationPolicyResolver（agent.Session 头 cwd+knob 折叠→service.Resolve），policy 服务缺位且 backend 不围栏时仍可工作。集成测验证：workspace-write 下根内 create 落盘、盘根 create 拒绝且编辑器包沙箱拒绝标记。
5. **tool-fs 工具族 + 沙箱升级词汇（官方 dsh-tool-fs 核心 + dsh-sandbox/escalation 全量）**：`sandbox/escalation.go`（两执行族共用的升级词汇唯一家——严格更宽阶梯 WIDER_MODES（read-only→workspace-write/danger、workspace-write→danger；封顶不再升）、闭集 ESCALATION_TARGETS、参数配对校验（permissions/justification 同进同出+非空句）、**逐字标记**：`[sandbox: file access denied under <mode> mode]` + 同轮升级提示标记、ApproveEscalation 有序 fail-closed 序列：先严格更宽检查（执行期检查而非 schema 约束）→ 无审批服务报错 → 无 agent fail-closed → 审批 ask（理由自含审计：`escalate sandbox to <mode>: <理由>`）→ 四值结果映射各自逐字报错，rogue 值 fail-closed；非更宽请求绝不打扰人类）；`toolfs` 包（read/write/edit 三工具：read 走 stat 路由→大文件/未知大小**流式**、buildWindow 行+字节双帽扫描仍数出精确总行数、单行截断标记、offset 越 EOF 报 FS_NOT_FOUND 官方文案、渲染 `<path>..</path>` 信封+续读 footer、langFromPath 扩展名提示；write 单槽意图 waterfall 默认无条件原子写、Created/Updated 文案、before 可空 oneOf schema；edit 字面唯一匹配（fslocal 纪律：0→EDIT_NOT_FOUND/多匹配→AMBIGUOUS_EDIT 带 more-specific 指引）、stale/not-observed 附补救文案（re-read/read the file then retry——FS 码保留）、executing agent 的 session cwd 为解析基（`..` 穿越即 canonical 化）、观察记录 fs/observed；执行体 canonical 值纯 JSON 形状（[]any/map[string]any，指针拒绝——lossless JSON 纪律）；**escalation 字段仅围栏 backend 挂载时进 schema**（validator 先拒）、FS_SANDBOX_DENIED→共享 marker+同轮提示、审批通道经 userapproval.Service.Request 适配（四值词汇与官方同构）；read_image 按**源规则自身**不注册：需 attachments 存储，Go 组合未挂载——如实记录）。catalog +1（累计 35）：dsh-tool-fs（Inject tools/fs/agents/systemPrompt/sandboxPolicy/userApproval；挂三段 systemPrompt 指引 section，order 1100/1200/1300）。集成断言：围栏组合下 write schema 带 sandbox_permissions/justification 枚举。
6. **subprocess 执行缝（官方 dsh-subprocess 契约 + dsh-subprocess-local 本地实现）**：`subprocess` 包——完全指定的 spawn 请求（argv 不经 shell、cwd/stdio/grace 全显式无默认，缺一 fail loud）、Node 形 stdio 逐流处置（stdin ignore/pipe/{data 批量写后关}；stdout/stderr pipe=裸流归调用者/inherit/collect）、**有界收集输出**：内存尾帽（溢出丢头保尾——错误与结果聚在输出末尾的 pi/OpenCode 理据）+ 可选全流 spill 文件（O_EXCL+随机名防预测防符号链接种植、超全流帽即弃 spill 断告、整流字节坐标 offset 游标零读指针——独立读者互不吞噬、lossy 读报截断并指路 spill）、spawn 上下文取消即 terminate、**树域终止**：POSIX setpgid 分离进程组 kill(-pgid) 组信号带直接子代回退（TERM→graceMs→KILL 阶梯；存活探针 kill(-pid,0) 仅 ESRCH 判死、EPERM 判活）、Windows taskkill /T /F 立即强杀（阶梯塌缩、结果有意不查——与 ESRCH 同等的容忍）、树退出观察者首个确认缺席即永久不再发信号（防 pid 复用误杀）、**drain 边界**：进程退出后幸存后代持有的继承管道由同一 grace 封顶（仅 harness 收集管道强关，pipe 模式流归调用者）、环境基座 scrubbedParentEnv（DSH_* 与 KEY/PASSWORD/SECRET/TOKEN 凭证形名全部剔除、大小写不敏感匹配 Windows 语义；显式 env 字符串=有意 opt-in 可还原凭证/DSH 事实、nil 墓碑=删普通环境项）；Go 适配：平台文件分治（tree_posix/tree_windows）、`SysProcAttr.Setpgid` 分离、exit 事实走 close 事件词汇（ExitCode/-1+Signal）、Runtime 接口 + Local 实现。官方 terminal 原语（pty 分配、前台组检视、win32 进程检查器）为**文档化缓期**非静默缺口——piped 实现已覆盖 batch/流式消费者。catalog +1（累计 36）：dsh-subprocess（Provide subprocess）。测试：批量 stdin 往返、管道 stdin 面、树终止阶梯（helper 进程 spin 循环 + 幂等再终止）、上下文取消联动、spec 逐项 fail loud、裸流归属、环境 scrub/墓碑、尾帽字节精确截断、spill 生命周期（创建→超帽弃→seal 后路径仍广播）。
7. **fs-search 发现工具族（官方 dsh-tool-fs-search：glob + grep + search-core）**：`fssearch` 包——两工具皆为经 subprocess 缝的前台固定 argv spawn（无 shell 层无引号问题：模式/include 走 `--flag=value`、目标躲在 `--` 后，前导 `-` 值永不成旗标；`--no-config` 前置防宿主 RIPGREP_CONFIG_PATH 注入 `--pre` 预处理器）；glob=`rg --files --glob=<模式> --sort=modified --no-ignore --hidden` + **每 VCS 名双否定 glob**（裸形遍历剪枝 + `/**` 形防搜索根恰在 .git 内时失效）、修改时间序契约（装得下就整列原样展示）、超帽页两种形态：修改时间头 or **跨顶层条目轮转采样**（每条目先得一席、桶内按轮追加、桶序随首现——展示页与卡片同法同算不分歧）、footer 三态（平尾/sampled-across 带收窄提示/完整结果可存 spill 时的恢复定位）；grep=行导向 `rg --json` NDJSON（path/line_number/lines 三字段无冒号切分歧义）、begin/end/context/summary 记录为传输框架跳过、**malformed 即 SEARCH_FAILED 绝不部分结果**、非 UTF-8 行（base64 bytes）出占位预览不炸全局、按文件首现序分组 `Line N: <text>`、行预览 UTF-8 边界保安全截断+`(line truncated)` 标记、`Found N of M matches` 预算事实头、include 纪律（空白拒绝/否定式拒绝/顶层逗号拒绝而 `{a,b}` 花括号交替合法）；错误词汇 SEARCH_INVALID_PATTERN（regex parse error/error parsing glob 分类）/SEARCH_FAILED（launch 失败/信号击杀/畸形输出）/SEARCH_RAW_OUTPUT_OVERFLOW（lossy 或超 rawOutputMaxBytes——**宁清晰失败不解析静默残流**）/SEARCH_ABORTED，exit 1=成功零结果、exit 0=有结果；session cwd 为 workdir、显示路径 workdir 相对化（界外原样透传——v1 部署要求 workdir 与 fs read 根同工作区，文档化非运行时校验）。**Go 部署适配（诚实记录）**：官方打包 @vscode/ripgrep，Go 缝从 PATH 惰性解析 rg（一次/进程、rgPath 配置可钉死）——缺二进制在首次调用 SEARCH_FAILED 而非 Loader 组装失败（与官方 call-boundary 解析同语义）；集成测在无 rg 部署上**如实 skip**（本机无 rg：spawn 全链路由部署验证）；spill 恢复面（trySaveFormattedResult）Go 尚无 spillStore——opportunistic ctx.Get 缺席走 could-not-save 文案与官方可选语义一致；search 卡片（presentationMeta）随展示轮。catalog +1（累计 37）：dsh-tool-fs-search（Inject tools/subprocess/systemPrompt；挂 tool:glob 1400/tool:grep 1500 指引段，超帽文案随采样开关）。
8. **shell 契约 + 受管环境注册表（官方 dsh-shell + dsh-shell-env：bash/pwsh 执行器缝的地基）**：`shell` 包——执行契约词汇（ShellExecRequest→ShellExecutor.Resolve→ShellExecSpec 的两段式：workdir/timeout 由实现默认+封顶后填充；Run **只对基础设施失败报错**——非零退出/超时击杀/中止击杀都是描述性结果；Aborted 与 TimedOut 互斥、单一融合 deadline 报首因不双报；Start 立即返回且无超时、done 永不报错、spawn 失败结为 killed+stderr 错误；ReadOutput 增量消费永不重发、lossy 指路 spill；后台进程归属组合拆卸边界=subprocess 服务 disposal、执行器级 reload 不杀）；受管环境合并序（先洗凭证、再请求方 env、最后 DSH_* 快照封顶——环境事实永不被顶替、宿主陈值永不透传）；ShellSandboxInfo 与退出状态独立上报（命令失败≠策略拒绝≠runner 失败）；ParseExitStatus 复演端出口药丸恢复（`[exit code: N]`/`[killed by signal: X]` 标记的正文剥离，消吃避免双重渲染，timeout/sandbox 标记留正文，要求前导换行+串尾防止 marker 样正文误匹配）。shell-env 注册表：内建事实 DSH_HOME/DSH_SHELL/DSH_SESSION_ID 注册表自持、插件贡献者声明键集（名字/键唯一、内建键保留、键形 `DSH_[A-Z][A-Z0-9_]*`、描述非空——全量 fail-loud 先于首命令）；collect 按名序解析、返回未声明键 panic 炸场；list 枚举不执行解析器。catalog +1（累计 38）：dsh-shell-env（Provide shellEnv；dshHome 配置）。**诚实记录**：DSH_SESSION_JSONL 持久化贡献者随 Go session/persistence 组合落地；shell 服务本体（bash-local/pwsh-local 执行器、tool-bash/tool-pwsh 工具面）按官方 win32 互斥装配——执行器两条已随条目 9 落地，工具两条已随条目 10 落地。
9. **本地 shell 执行器（官方 dsh-bash-local + dsh-pwsh-local：`ctx.shell` 的两个提供者）**：`shelllocal` 包——公共命令 `bash -c` / PowerShell（UTF-8 双向 preamble + `-NoLogo -NoProfile -NonInteractive -Command`；可执行文件解析=pwshPath 钉死→PS7 已知位置→PATH 条目（setx 式整条引号剥离、lstat 语义看得见 Store 别名不踩 ACL）→5.1 兜底→POSIX 交 PATH）；请求/规范两段（缺省 cwd=config→进程 cwd、timeout 缺省=封顶；stdoutMaxBytes 信任内进程消费者直传）；**超时/中止单首因分类**（CAS 记先到因，双臂竞态只报一个）；后台句柄=立即返回、无超时、done 永不报错、信号终止（含自杀）结 killed、spawn 失败结 killed 且失败注记**恰一次**经读路径送达（与真 stderr 互斥）；读路径增量消费永不重发、stderr 以 `[stderr]\n` 分节、节间单换行补齐；模型友好终端环境（NO_COLOR/TERM=dumb/PAGER/GIT_PAGER）层序=覆盖先行→调用方 env→DSH_* 快照封顶（受管事实不可顶替）；bounded collect+spill 上限每 spawn 显式供给（后台进程跨执行器 reload 仍受管——拆卸边界在 subprocess 服务 disposal）。catalog +2（累计 40/86，余 46）：dsh-bash-local + dsh-pwsh-local（皆 Provide shell；官方语义一宿主一提供者、双装 fail-loud，win32 层换行——本机 headless 实跑走 pwsh 行）。**测试部署适配（如实）**：Windows 的 PATH "bash" 是 WSL stub（官方 tool-bash 亦 win32 禁用）——bash 生命周期真跑测仅 POSIX 生效，win32 以 pwsh 执行器跑同套真 spawn 生命周期（运行/超时击杀/中止分类/后台增量读/取消击杀，本机全过）。
10. **模型面 shell 工具（官方 dsh-tool-bash + dsh-tool-pwsh：`bash`/`pwsh` 工具与 `tool:bash`/`tool:pwsh` 指引段）**：`shelltool` 包——一个注册函数按所组合执行器的 flavor 派生工具身份（`Name()=="pwsh-local"` → `pwsh`/OrderToolPwsh/`[exit code: N]`+裸 exit 1 即中断的指引段，否则 `bash`/OrderToolBash）；参数面 command/description（必填 trim 非空）+ timeoutMs（正数校验逐字 `invalid timeoutMs: expected a positive number, got …`）+ workdir（模型相对路径接会话 cwd——调用方 agent 经 scope→Agent 解析器取 `Session.Header().CWD`，缺省即会话根）+ `run_in_background`（默认开，`enableRunInBackground: false` 时**不进 schema 且调用拒绝**逐字禁用文案）；执行体=Env.Collect（受管 DSH_* 快照入请求）→ Resolve→Run（Signal=调用方取消）；仅基础设施失败报错，非零退出/超时/信号都是描述性 canonical 值（`exitCode` 信号时 null、`signal` 空时 null、spillPath 非空才在、timedOut/aborted/timeoutMs/stdout/stderr{text,truncated,spillPath}，输出 schema OneOf background{kind,jobId}/foreground）；渲染=stdout→`[stderr]` 分节→尾部标记阶梯（timedOut→signal→exit code，exit 锚最后）+ 截断指路 spill（缺路径报 `(unavailable)`）；后台经 jobs 缝：StartSpec{kind bash/pwsh, label=命令, owner=调用方 agent 适配（无 agent 即 unowned）}、processOutcome 映射 killed→killed（signal detail）/其余→completed+exit detail（非零退出照 foreground 语义是报告不是失败）、无 jobs 服务报 `background jobs unavailable`、调用方取消预检→`tool call aborted`。catalog +2（累计 42/86，余 44）：dsh-tool-bash + dsh-tool-pwsh（Inject tools/shell/systemPrompt/shellEnv/jobs/agents；共享 buildShellTool 工厂；jobs 为可选注入——缺位即 pending 等待而非静默）。**诚实记录**：官方 escalation 子句（sandbox_permissions/justification 与 confining 执行器沙箱通知）不移植——Go 组合无受限 shell 执行器，字段不进 schema 标记不可达；`Cancel` 钩子为空操作（Go 后台进程无独立 kill 谓词，停止走 job Kill 通道的 spec.Signal）；boot 组装测按宿主条件装配恰好一行（win32=pwsh-local+tool-pwsh，POSIX=bash-local+tool-bash），双行装配被 prompt section 重名 fail-loud 挡住——组合纪律的测试化。
11. **插件 ABI**：宿主端 TS 插件改为受管子进程（stdio + JSON-RPC），TS 插件零改动迁移。
